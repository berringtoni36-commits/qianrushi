import { AbsoluteFill, Composition, Easing, interpolate, Sequence, useCurrentFrame } from "remotion";
import type { CSSProperties, ReactNode } from "react";

const FPS = 30;
const seconds = (value: number) => Math.round(value * FPS);
const COLORS = { background: "#eef2f4", surface: "rgba(255, 255, 255, 0.72)", strong: "rgba(255, 255, 255, 0.90)", text: "#100f0f", muted: "#6f6e69", faint: "#9b9a97", border: "rgba(22, 30, 38, 0.11)", accent: "#d87757", active: "#087f8c", result: "#198754", danger: "#c2413b", compare: "#b45309", violet: "#6f65b8" };
const BODY_FONT = 'Inter, "PingFang SC", "Noto Sans SC", system-ui, sans-serif';
const DISPLAY_FONT = 'Iowan Old Style, "Songti SC", STSong, Georgia, serif';
const CODE_FONT = '"Fira Code", "SFMono-Regular", Menlo, Consolas, monospace';
const ease = Easing.bezier(0.22, 1, 0.36, 1);
const SCENE = { opening: seconds(16), map: seconds(18), string: seconds(44), buffer: seconds(40), traps: seconds(34), followups: seconds(48), final: seconds(26) } as const;
const TOTAL_FRAMES = Object.values(SCENE).reduce((sum, value) => sum + value, 0);
const clamp = (value: number, start: number, end: number) => interpolate(value, [start, end], [0, 1], { extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: ease });
const reveal = (frame: number, start = 0, distance = 16): CSSProperties => { const immediate = start <= 0; const from = immediate ? 0 : start; return { opacity: interpolate(frame, [from, from + 24], immediate ? [0.9, 1] : [0, 1], { extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: ease }), transform: `translateY(${interpolate(frame, [from, from + 24], immediate ? [3, 0] : [distance, 0], { extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: ease })}px)` }; };
const Card: React.FC<{ children: ReactNode; style?: CSSProperties; strong?: boolean }> = ({ children, style, strong }) => <div style={{ border: `1px solid ${COLORS.border}`, borderRadius: 8, backgroundColor: strong ? COLORS.strong : COLORS.surface, boxShadow: "0 16px 48px rgba(22, 30, 38, 0.10)", ...style }}>{children}</div>;
const Background: React.FC = () => <><div style={{ position: "absolute", inset: 0, backgroundColor: COLORS.background, backgroundImage: "linear-gradient(rgba(8, 127, 140, 0.035) 1px, transparent 1px), linear-gradient(90deg, rgba(8, 127, 140, 0.035) 1px, transparent 1px), linear-gradient(135deg, rgba(255,255,255,0.42), rgba(238,242,244,0.18))", backgroundSize: "48px 48px, 48px 48px, 100% 100%" }} /><div style={{ position: "absolute", left: -180, top: 80, width: 760, height: 760, borderRadius: 180, backgroundColor: "rgba(255, 255, 255, 0.28)", transform: "rotate(-16deg)", filter: "blur(12px)" }} /><div style={{ position: "absolute", right: -240, bottom: -260, width: 760, height: 680, borderRadius: 220, backgroundColor: "rgba(216, 119, 87, 0.075)", transform: "rotate(18deg)", filter: "blur(16px)" }} /></>;
const TopBar: React.FC<{ frame: number; section: string; title: string; tag?: string }> = ({ frame, section, title, tag = "慢速复习 · 3:46" }) => <Card strong style={{ position: "absolute", left: 58, right: 58, top: 26, minHeight: 62, padding: "0 20px", display: "flex", alignItems: "center", justifyContent: "space-between", gap: 20, ...reveal(frame) }}><div style={{ display: "flex", alignItems: "center", gap: 14, minWidth: 0 }}><div style={{ color: COLORS.accent, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800 }}>C / C++ 基础</div><div style={{ width: 1, height: 18, backgroundColor: COLORS.border }} /><div style={{ color: COLORS.text, fontSize: 18, fontWeight: 760, whiteSpace: "nowrap" }}>{section}</div><div style={{ color: COLORS.muted, fontSize: 16, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{title}</div></div><div style={{ flex: "0 0 auto", padding: "8px 11px", border: `1px solid ${COLORS.border}`, borderRadius: 6, color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 700 }}>{tag}</div></Card>;
const Caption: React.FC<{ frame: number; label: string; title: string; body: string; color?: string }> = ({ frame, label, title, body, color = COLORS.active }) => <Card strong style={{ position: "absolute", left: 58, right: 58, bottom: 54, minHeight: 106, padding: "15px 20px 16px", borderTop: `3px solid ${color}`, ...reveal(frame) }}><div style={{ display: "flex", justifyContent: "space-between", color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 11, fontWeight: 800 }}><span>{label}</span><span>先理解，再记忆</span></div><div style={{ marginTop: 8, color: COLORS.text, fontSize: 22, fontWeight: 800 }}>{title}</div><div style={{ marginTop: 5, color: COLORS.muted, fontSize: 15, lineHeight: 1.45 }}>{body}</div></Card>;
const SceneTitle: React.FC<{ frame: number; eyebrow: string; title: string; detail: string }> = ({ frame, eyebrow, title, detail }) => <div style={{ position: "absolute", left: 72, right: 72, top: 122, ...reveal(frame) }}><div style={{ color: COLORS.accent, fontFamily: CODE_FONT, fontSize: 14, fontWeight: 800, letterSpacing: 1.3 }}>{eyebrow}</div><div style={{ marginTop: 10, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 44, fontWeight: 700, lineHeight: 1.16 }}>{title}</div><div style={{ marginTop: 9, color: COLORS.muted, fontSize: 18, lineHeight: 1.45 }}>{detail}</div></div>;
const Prompt: React.FC<{ frame: number; question: string; seconds: number; color?: string; hint?: string }> = ({ frame, question, seconds: duration, color = COLORS.active, hint = "先在脑中说一遍，再看标准说法" }) => { const remaining = Math.max(0, Math.ceil((duration * FPS - frame) / FPS)); const progress = clamp(frame, 0, duration * FPS); return <Card strong style={{ width: 670, minHeight: 290, padding: "28px 32px", borderLeft: `4px solid ${color}`, ...reveal(frame, 0, 20) }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800, letterSpacing: 1.2 }}>先暂停 · 主动回忆</div><div style={{ marginTop: 20, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 33, fontWeight: 700, lineHeight: 1.28 }}>{question}</div><div style={{ display: "flex", alignItems: "center", gap: 18, marginTop: 26 }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 44, fontWeight: 800, minWidth: 54 }}>{remaining}</div><div style={{ color: COLORS.muted, fontSize: 16, lineHeight: 1.45 }}>{hint}</div></div><div style={{ height: 6, marginTop: 24, borderRadius: 999, backgroundColor: COLORS.border }}><div style={{ height: "100%", width: `${progress * 100}%`, borderRadius: 999, backgroundColor: color }} /></div></Card>; };
const CodePanel: React.FC<{ title: string; lines: string[]; activeLine?: number; style?: CSSProperties }> = ({ title, lines, activeLine = -1, style }) => <Card strong style={{ overflow: "hidden", backgroundColor: "rgba(247,248,249,0.92)", ...style }}><div style={{ height: 44, display: "flex", alignItems: "center", gap: 8, padding: "0 16px", borderBottom: `1px solid ${COLORS.border}`, color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 750 }}><span style={{ width: 8, height: 8, borderRadius: 999, backgroundColor: COLORS.danger }} /><span style={{ width: 8, height: 8, borderRadius: 999, backgroundColor: COLORS.compare }} /><span style={{ width: 8, height: 8, borderRadius: 999, backgroundColor: COLORS.result }} /><span style={{ marginLeft: 8 }}>{title}</span></div><div style={{ padding: "18px 18px", fontFamily: CODE_FONT, fontSize: 19, lineHeight: 1.72 }}>{lines.map((line, index) => { const active = index === activeLine; return <div key={`${title}-${index}`} style={{ display: "flex", minHeight: 34, padding: "0 10px", borderRadius: 5, backgroundColor: active ? `${COLORS.compare}16` : "transparent", color: active ? COLORS.text : index === 0 ? COLORS.muted : COLORS.text, opacity: activeLine >= 0 && index > activeLine + 2 ? 0.44 : 1 }}><span style={{ width: 31, color: active ? COLORS.compare : COLORS.faint, fontSize: 14 }}>{String(index + 1).padStart(2, "0")}</span><span style={{ whiteSpace: "pre" }}>{line}</span></div>; })}</div></Card>;
const Badge: React.FC<{ children: ReactNode; color: string }> = ({ children, color }) => <div style={{ display: "inline-flex", alignItems: "center", gap: 8, padding: "8px 11px", border: `1px solid ${color}55`, borderRadius: 6, backgroundColor: `${color}12`, color, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 750 }}><span style={{ width: 7, height: 7, borderRadius: 999, backgroundColor: color }} />{children}</div>;

const Opening: React.FC = () => { const frame = useCurrentFrame(); const done = frame >= seconds(6); const cards = [["01", "内存大小", "sizeof 看对象或类型占多少字节", COLORS.active], ["02", "字符串长度", "strlen 扫描到 \\0 之前的字符数", COLORS.result], ["03", "缓冲区边界", "二进制和 DMA 用显式 length", COLORS.danger]] as const; return <AbsoluteFill><TopBar frame={frame} section="第 7 题" title="sizeof 和 strlen 有什么区别？" /><SceneTitle frame={frame} eyebrow="复习目标" title="先问：你要的是内存大小，还是字符串长度？" detail="两个名字都像是在‘数长度’，但一个看对象占用的字节数，一个扫描 C 字符串的终止符。" /><div style={{ position: "absolute", left: 72, top: 304, right: 72, display: "flex", gap: 26, alignItems: "stretch" }}><Prompt frame={frame} question="sizeof 和 strlen，分别在数什么？" seconds={6} color={COLORS.active} hint="先分开：字节数、字符数、结束条件" /><Card strong style={{ flex: 1, padding: "28px 30px", opacity: done ? 1 : 0.42, transform: `translateX(${done ? 0 : 18}px)` }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800, letterSpacing: 1.2 }}>本题路线</div><div style={{ marginTop: 22, display: "grid", gap: 16 }}>{cards.map(([number, label, detail, color]) => <div key={number} style={{ display: "grid", gridTemplateColumns: "44px 1fr", gap: 12, alignItems: "center", padding: "12px 14px", border: `1px solid ${COLORS.border}`, borderRadius: 8, backgroundColor: `${color}0b` }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 15, fontWeight: 800 }}>{number}</div><div><div style={{ color: COLORS.text, fontSize: 20, fontWeight: 800 }}>{label}</div><div style={{ marginTop: 4, color: COLORS.muted, fontSize: 14 }}>{detail}</div></div></div>)}</div></Card></div><Caption frame={frame} label="本题总纲" title="sizeof 看占用字节数，strlen 看 0x00 之前的字符数" body="sizeof 不把自己当字符串函数；strlen 也不能用来测二进制缓冲区的真实长度。" /></AbsoluteFill>; };

const MAP = [["sizeof", "sizeof(str)", "编译器运算符，关注对象/类型的字节数", COLORS.active], ["strlen", "strlen(str)", "运行时扫描，遇到 \\0 停止且不计入 \\0", COLORS.result], ["工程边界", "buf + len", "二进制、DMA、协议帧用显式长度", COLORS.danger]] as const;
const MapScene: React.FC = () => { const frame = useCurrentFrame(); return <AbsoluteFill><TopBar frame={frame} section="第 7 题" title="字节数、字符串长度和缓冲区边界" /><SceneTitle frame={frame} eyebrow="答案地图" title="先辨认对象，再决定是否需要终止符" detail="sizeof 可以接受类型或表达式；strlen 只适合以 0x00 结尾的 C 字符串；底层数据通常自带长度。" /><div style={{ position: "absolute", left: 72, right: 72, top: 306, display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 18 }}>{MAP.map(([title, code, detail, color], index) => <Card key={title} strong style={{ minHeight: 300, padding: "26px 24px", borderTop: `4px solid ${color}`, ...reveal(frame, index * 26, 22) }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>{String(index + 1).padStart(2, "0")}</div><div style={{ marginTop: 22, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 31, fontWeight: 700 }}>{title}</div><div style={{ marginTop: 28, padding: "12px 14px", borderRadius: 6, backgroundColor: `${color}10`, color, fontFamily: CODE_FONT, fontSize: 16 }}>{code}</div><div style={{ marginTop: 16, color: COLORS.muted, fontSize: 16, lineHeight: 1.45 }}>{detail}</div></Card>)}</div><Caption frame={frame} label="答案框架" title="sizeof 先看类型，strlen 先找终止符" body="接下来用同一组 str 和 p，追踪数组、指针、0x00 和显式长度如何产生不同结果。" color={COLORS.compare} /></AbsoluteFill>; };

const STRING_STATES = [["字符数组", "char str[] = \"abc\";", "数组包含 a、b、c 和结尾的 \\0，共 4 个 char", COLORS.accent], ["sizeof 数组", "sizeof(str)", "得到整个数组的字节数：4", COLORS.active], ["strlen 扫描", "strlen(str)", "运行时扫描，\\0 之前有 3 个字符", COLORS.result], ["指针别名", "char *p = str;", "p 指向同一串内容，但 p 自己是指针变量", COLORS.compare], ["sizeof 指针", "sizeof(p)", "得到 sizeof(char*)，具体宽度看目标平台", COLORS.danger]] as const;
const StringScene: React.FC = () => { const frame = useCurrentFrame(); const promptDone = frame >= seconds(7); const phase = promptDone ? Math.min(4, Math.floor((frame - seconds(7)) / seconds(7))) : -1; const activeLine = phase < 0 ? 0 : [0, 1, 2, 3, 4][phase]; return <AbsoluteFill><TopBar frame={frame} section="第 7 题 / 字符串" title="同一个 str，sizeof 和 strlen 看见的不是同一件事" /><SceneTitle frame={frame} eyebrow="确定性轨迹" title="数组把 0x00 算进空间，strlen 把 0x00 当终点" detail={'`char str[] = "abc"` 有四个 char 的存储；strlen 只报告终止符之前的三个字符。'} /><div style={{ position: "absolute", left: 72, right: 72, top: 300, height: 580, display: "grid", gridTemplateColumns: "0.88fr 1.12fr", gap: 22 }}><CodePanel title="string_length.c" activeLine={activeLine} style={{ height: 520 }} lines={["char str[] = \"abc\";", "sizeof(str) = 4;", "strlen(str) = 3;", "char *p = str;", "sizeof(p) = sizeof(char *);", "strlen(p) = 3;", "// p 不是数组本体"]} /><Card strong style={{ padding: "18px 18px", position: "relative" }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 11, fontWeight: 800 }}>字符串关系 · 当前阶段 {phase < 0 ? "等待回忆" : `${phase + 1} / 5`}</div><div style={{ marginTop: 12, display: "grid", gap: 6 }}>{STRING_STATES.map(([title, code, detail, color], index) => { const active = index === phase; return <div key={title} style={{ padding: "8px 11px", border: `1px solid ${active ? color : COLORS.border}`, borderTop: `3px solid ${color}`, borderRadius: 7, backgroundColor: active ? `${color}12` : "rgba(255,255,255,0.52)", ...reveal(frame, 18 + index * 17) }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 10 }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 14, fontWeight: 800 }}>{title}</div><Badge color={color}>{active ? "当前" : "关系"}</Badge></div><div style={{ marginTop: 4, color: COLORS.text, fontFamily: CODE_FONT, fontSize: 13 }}>{code}</div><div style={{ marginTop: 3, color: COLORS.muted, fontSize: 12.5, lineHeight: 1.25 }}>{detail}</div></div>; })}</div>{!promptDone && <div style={{ position: "absolute", inset: 18, display: "flex", alignItems: "center", justifyContent: "center", backgroundColor: "rgba(255,255,255,0.74)", borderRadius: 8 }}><Prompt frame={frame} question={'`char str[] = "abc"` 时，sizeof(str) 和 strlen(str) 各是多少？'} seconds={7} color={COLORS.active} hint="先数数组空间，再找字符串终止符" /></div>}</Card></div><Caption frame={frame} label="字符串口诀" title="数组结果是 4，字符串结果是 3" body="把 str 交给指针 p 后，strlen(p) 仍然是 3，但 sizeof(p) 只得到指针变量大小。" /></AbsoluteFill>; };

const BYTE_CELLS = [["0x41", "A", COLORS.active], ["0x00", "\\0", COLORS.danger], ["0x42", "B", COLORS.compare]] as const;
const BUFFER_STATES = [["缓冲区已知长度", "payload 有 3 个字节", "0x00 也是合法数据，不代表缓冲区结束", COLORS.accent], ["先读 0x41", "strlen 已计 1", "第一个字节不是 \\0，扫描继续", COLORS.active], ["遇到 0x00", "strlen 停在 1", "strlen 把 0x00 当字符串终点，后面的 0x42 不再计数", COLORS.danger], ["显式传长度", "send(payload, 3)", "二进制、DMA 和协议帧使用真实长度，不依赖 strlen", COLORS.result]] as const;
const BufferScene: React.FC = () => { const frame = useCurrentFrame(); const promptDone = frame >= seconds(6); const phase = promptDone ? Math.min(3, Math.floor((frame - seconds(6)) / seconds(8))) : -1; const activeLine = phase < 0 ? 0 : [0, 3, 3, 5][phase]; const activeByte = phase === 1 ? 0 : phase === 2 ? 1 : phase === 3 ? 2 : -1; return <AbsoluteFill><TopBar frame={frame} section="第 7 题 / 缓冲区" title="二进制数据不能靠 strlen 猜长度" /><SceneTitle frame={frame} eyebrow="嵌入式边界" title="0x00 可能是数据，不是结束标记" detail="C 字符串把 0x00 当边界；协议帧、DMA 缓冲区和二进制 payload 必须携带显式长度。" /><div style={{ position: "absolute", left: 72, right: 72, top: 300, height: 580, display: "grid", gridTemplateColumns: "0.92fr 1.08fr", gap: 22 }}><CodePanel title="binary_buffer.c" activeLine={activeLine} style={{ height: 520 }} lines={["uint8_t payload[] = {0x41, 0x00, 0x42};", "const char *text = \"ABC\";", "strlen(text) = 3;", "strlen((const char *)payload) = 1;", "size_t len = sizeof(payload);", "send(payload, len);", "// 二进制长度不能靠 strlen"]} /><Card strong style={{ padding: "24px 24px", position: "relative" }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>扫描轨迹 · 当前阶段 {phase < 0 ? "等待回忆" : `${phase + 1} / 4`}</div><div style={{ marginTop: 22, display: "flex", gap: 12 }}>{BYTE_CELLS.map(([hex, label, color], index) => <div key={hex} style={{ flex: 1, minWidth: 0, padding: "18px 12px", border: `2px solid ${index === activeByte ? color : COLORS.border}`, borderRadius: 8, backgroundColor: index === activeByte ? `${color}14` : "rgba(255,255,255,0.52)", ...reveal(frame, 18 + index * 18) }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 18, fontWeight: 800, textAlign: "center" }}>{hex}</div><div style={{ marginTop: 12, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 30, fontWeight: 700, textAlign: "center" }}>{label}</div><div style={{ marginTop: 10, color: index === 1 ? COLORS.danger : COLORS.muted, fontSize: 13, textAlign: "center" }}>{index === 1 ? "字符串终点" : "有效字节"}</div></div>)}</div><div style={{ marginTop: 26, padding: "18px 18px", borderLeft: `4px solid ${phase === 2 ? COLORS.danger : COLORS.result}`, borderRadius: 7, backgroundColor: phase === 2 ? `${COLORS.danger}10` : `${COLORS.result}10` }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>当前结论</div><div style={{ marginTop: 10, color: COLORS.text, fontSize: 24, fontWeight: 800 }}>{phase < 0 ? "实际缓冲区长度 = 3" : BUFFER_STATES[phase][1]}</div><div style={{ marginTop: 8, color: COLORS.muted, fontSize: 16, lineHeight: 1.45 }}>{phase < 0 ? "先观察 0x00 的位置，再判断 strlen 会在哪里停下。" : BUFFER_STATES[phase][2]}</div></div><div style={{ marginTop: 28, display: "flex", justifyContent: "space-between", alignItems: "center", color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 13 }}><span>C 字符串视角</span><span style={{ color: COLORS.danger, fontWeight: 800 }}>遇到 0x00 就停止</span></div>{!promptDone && <div style={{ position: "absolute", inset: 22, display: "flex", alignItems: "center", justifyContent: "center", backgroundColor: "rgba(255,255,255,0.74)", borderRadius: 8 }}><Prompt frame={frame} question="payload = {0x41, 0x00, 0x42}，strlen 会返回 3 吗？" seconds={6} color={COLORS.danger} hint="区分真实字节数和 C 字符串长度" /></div>}</Card></div><Caption frame={frame} label="嵌入式口诀" title="二进制传输：指针 + 显式长度" body="strlen(payload) 只能得到遇到第一个 0x00 之前的片段长度，DMA 计数器或协议帧长才是边界。" color={COLORS.danger} /></AbsoluteFill>; };

const TRAPS = [["函数参数已退化", "void inspect(const char buf[])", "函数内 sizeof(buf) 得到指针大小，不是原数组大小", COLORS.active], ["二进制含 0x00", "strlen(buf)", "遇到第一个 0x00 就停，不能代表 payload 长度", COLORS.danger], ["VLA 是例外", "sizeof(vla)", "变长数组的 sizeof 可能在运行时计算", COLORS.compare], ["空类型要谨慎", "sizeof(empty)", "ISO C 不定义空结构体；C++ 空类对象大小需非零", COLORS.violet]] as const;
const TrapsScene: React.FC = () => { const frame = useCurrentFrame(); const promptDone = frame >= seconds(5); const phase = promptDone ? Math.min(3, Math.floor((frame - seconds(5)) / seconds(7))) : -1; return <AbsoluteFill><TopBar frame={frame} section="第 7 题 / 易错点" title="四个边界，决定你会不会把它们混在一起" /><SceneTitle frame={frame} eyebrow="高频陷阱" title="不要把‘大多数情况’说成‘永远如此’" detail="数组参数、二进制数据、变长数组和空类型，分别挑战不同的直觉。" /><div style={{ position: "absolute", left: 72, right: 72, top: 306, display: "grid", gridTemplateColumns: "1fr 1fr", gap: 18 }}>{TRAPS.map(([title, code, detail, color], index) => { const active = index === phase; return <Card key={title} strong style={{ minHeight: 225, padding: "24px 24px", borderTop: `4px solid ${color}`, borderColor: active ? color : COLORS.border, backgroundColor: active ? `${color}10` : COLORS.strong, ...reveal(frame, index * 14, 18) }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 14 }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800 }}>{String(index + 1).padStart(2, "0")}</div><Badge color={color}>{active ? "当前边界" : "易错点"}</Badge></div><div style={{ marginTop: 16, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 25, fontWeight: 700 }}>{title}</div><div style={{ marginTop: 14, color, fontFamily: CODE_FONT, fontSize: 15, fontWeight: 800 }}>{code}</div><div style={{ marginTop: 10, color: COLORS.muted, fontSize: 15, lineHeight: 1.4 }}>{detail}</div></Card>; })}</div>{!promptDone && <div style={{ position: "absolute", left: 625, top: 368 }}><Prompt frame={frame} question="sizeof 总是在编译期完成吗？strlen 能测任意缓冲区吗？" seconds={5} color={COLORS.compare} hint="先说常见规则，再补标准边界" /></div>}<Caption frame={frame} label="边界总结" title="sizeof 有 VLA 例外，strlen 有 C 字符串前提" body="回答面试追问时，先给主规则，再补‘变长数组’和‘二进制含 0x00’这两个关键边界。" color={COLORS.compare} /></AbsoluteFill>; };

const FOLLOWUPS = [["sizeof 是编译期还是运行时？", "普通对象和类型的 sizeof 通常可在翻译期确定；C 的变长数组是重要例外，运行时才知道大小。", COLORS.active], ["str 和 p 的 sizeof 为什么不同？", "char str[] = \"abc\" 有 4 个 char；char *p = str 时，sizeof(p) 只得到指针变量大小，但 strlen(p) 仍为 3。", COLORS.accent], ["为什么二进制不能随便 strlen？", "0x00 可能是有效数据，strlen 会把它当 C 字符串终点，导致长度被提前截断。", COLORS.danger], ["DMA 接收长度怎么判断？", "读取 DMA 计数器、协议帧长或环形缓冲区索引，并把指针和显式长度一起传给处理函数。", COLORS.result]] as const;
const FollowupScene: React.FC = () => { const frame = useCurrentFrame(); const index = Math.min(3, Math.floor(frame / seconds(12))); const local = frame - index * seconds(12); const answer = local >= seconds(5); const [question, explanation, color] = FOLLOWUPS[index]; return <AbsoluteFill><TopBar frame={frame} section="第 7 题 / 追问" title="面试官常问的四个边界问题" tag="慢速复习 · 0:48" /><SceneTitle frame={frame} eyebrow={`追问 ${index + 1} / 4`} title="先回答，再看标准说法" detail="每个追问先留 5 秒，答案只保留面试中真正需要说清的边界。" /><div style={{ position: "absolute", left: 72, right: 72, top: 302, display: "grid", gridTemplateColumns: "1.55fr 0.95fr", gap: 22 }}><Card strong style={{ minHeight: 570, padding: "34px 38px", borderLeft: `4px solid ${color}`, ...reveal(frame) }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800, letterSpacing: 1.2 }}>INTERVIEW FOLLOW-UP</div><div style={{ marginTop: 26, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 35, fontWeight: 700, lineHeight: 1.28 }}>{question}</div>{!answer ? <><div style={{ display: "flex", alignItems: "center", gap: 18, marginTop: 30 }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 46, fontWeight: 800 }}>{Math.max(0, Math.ceil((seconds(5) - local) / FPS))}</div><div style={{ color: COLORS.muted, fontSize: 17 }}>不要只说一个关键词，补上为什么。</div></div><div style={{ height: 6, marginTop: 24, borderRadius: 999, backgroundColor: COLORS.border }}><div style={{ height: "100%", width: `${clamp(local, 0, seconds(5)) * 100}%`, backgroundColor: color, borderRadius: 999 }} /></div></> : <div style={{ marginTop: 34, padding: "20px 22px", border: `1px solid ${color}44`, borderRadius: 7, backgroundColor: `${color}10`, color: COLORS.text, fontSize: 21, fontWeight: 750, lineHeight: 1.55, ...reveal(local, seconds(5)) }}>{explanation}</div>}</Card><Card strong style={{ minHeight: 570, padding: "26px 24px" }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>REVIEW QUEUE</div><div style={{ marginTop: 18, display: "grid", gap: 12 }}>{FOLLOWUPS.map(([item], itemIndex) => <div key={item} style={{ padding: "14px 14px", border: `1px solid ${itemIndex === index ? color : COLORS.border}`, borderRadius: 7, backgroundColor: itemIndex === index ? `${color}10` : "rgba(255,255,255,0.48)", opacity: itemIndex > index ? 0.58 : 1 }}><div style={{ color: itemIndex === index ? color : COLORS.faint, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>{String(itemIndex + 1).padStart(2, "0")}</div><div style={{ marginTop: 7, color: COLORS.text, fontSize: 16, lineHeight: 1.4 }}>{item}</div></div>)}</div></Card></div><Caption frame={frame} label="面试追问 · 复述" title="先说‘数的是什么’，再说计算时机和边界" body="不要只背‘sizeof 编译期、strlen 运行时’；VLA、数组退化和二进制 0x00 才是追问的落点。" color={color} /></AbsoluteFill>; };

const FINAL_ITEMS = [["一句话本质？", "sizeof 看对象或类型占多少字节；strlen 看 \\0 之前有多少字符。", COLORS.accent], ["\"abc\" 的两个结果？", "char str[] = \"abc\" 时，sizeof(str) = 4，strlen(str) = 3。", COLORS.active], ["换成 char *p 呢？", "sizeof(p) 是指针变量大小，strlen(p) 仍然是 3。", COLORS.compare], ["嵌入式怎么选？", "二进制、DMA 和协议帧使用显式长度，不用 strlen 猜边界。", COLORS.danger]] as const;
const FinalScene: React.FC = () => { const frame = useCurrentFrame(); const unit = seconds(6.5); const index = Math.min(3, Math.floor(frame / unit)); const local = frame - index * unit; const answer = local >= seconds(4.2); const [question, answerText, color] = FINAL_ITEMS[index]; return <AbsoluteFill><TopBar frame={frame} section="第 7 题 / 收束" title="最后一次：不看答案，自己复述" tag="慢速复习 · 0:26" /><SceneTitle frame={frame} eyebrow="记忆收束" title="把 sizeof 和 strlen 压缩成四个可复述的句子" detail="能说清本质、abc 示例、指针变化和二进制边界，这道题才算真正复习完成。" /><div style={{ position: "absolute", left: 72, right: 72, top: 306, display: "grid", gridTemplateColumns: "1.1fr 0.9fr", gap: 22 }}><Card strong style={{ minHeight: 565, padding: "32px 38px", borderLeft: `4px solid ${color}` }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800 }}>FINAL RECALL · {index + 1}/4</div><div style={{ marginTop: 26, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 35, fontWeight: 700, lineHeight: 1.28 }}>{question}</div>{!answer ? <div style={{ marginTop: 28, color: COLORS.muted, fontSize: 17 }}>先停 4.2 秒，再继续。</div> : <div style={{ marginTop: 32, padding: "18px 20px", border: `1px solid ${color}44`, borderRadius: 7, backgroundColor: `${color}10`, color: COLORS.text, fontSize: 21, fontWeight: 750, lineHeight: 1.55, ...reveal(local, seconds(4.2)) }}>{answerText}</div>}</Card><Card strong style={{ minHeight: 565, padding: "26px 24px" }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>ONE-LINE SUMMARY</div><div style={{ marginTop: 28, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 34, fontWeight: 700 }}>size ≠ string length</div><div style={{ marginTop: 22, display: "grid", gap: 12 }}>{[["sizeof", "对象 / 类型的字节数"], ["strlen", "\\0 之前的字符数"], ["buffer", "指针 + 显式长度"]].map(([label, text], itemIndex) => <div key={label} style={{ padding: "15px 16px", border: `1px solid ${itemIndex === index ? color : COLORS.border}`, borderRadius: 7, backgroundColor: itemIndex === index ? `${color}10` : "rgba(255,255,255,0.48)" }}><div style={{ color: itemIndex === index ? color : COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>{label}</div><div style={{ marginTop: 6, color: COLORS.text, fontSize: 17, fontWeight: 700 }}>{text}</div></div>)}</div></Card></div><Caption frame={frame} label="复习完成" title="能说清这四句，第 7 题就真正过了一遍" body="下次看到长度问题，先问‘对象本身有多大’，再问‘字符串在哪里结束’。" color={COLORS.result} /></AbsoluteFill>; };

export const SizeofStrlenQ7Video: React.FC = () => { const frame = useCurrentFrame(); const progress = interpolate(frame, [0, TOTAL_FRAMES - 1], [0, 1], { extrapolateLeft: "clamp", extrapolateRight: "clamp" }); const starts = { opening: 0, map: SCENE.opening, string: SCENE.opening + SCENE.map, buffer: SCENE.opening + SCENE.map + SCENE.string, traps: SCENE.opening + SCENE.map + SCENE.string + SCENE.buffer, followups: SCENE.opening + SCENE.map + SCENE.string + SCENE.buffer + SCENE.traps, final: SCENE.opening + SCENE.map + SCENE.string + SCENE.buffer + SCENE.traps + SCENE.followups }; return <AbsoluteFill style={{ fontFamily: BODY_FONT, color: COLORS.text, overflow: "hidden" }}><Background /><Sequence from={starts.opening} durationInFrames={SCENE.opening}><Opening /></Sequence><Sequence from={starts.map} durationInFrames={SCENE.map}><MapScene /></Sequence><Sequence from={starts.string} durationInFrames={SCENE.string}><StringScene /></Sequence><Sequence from={starts.buffer} durationInFrames={SCENE.buffer}><BufferScene /></Sequence><Sequence from={starts.traps} durationInFrames={SCENE.traps}><TrapsScene /></Sequence><Sequence from={starts.followups} durationInFrames={SCENE.followups}><FollowupScene /></Sequence><Sequence from={starts.final} durationInFrames={SCENE.final}><FinalScene /></Sequence><div style={{ position: "absolute", left: 58, right: 58, bottom: 22, height: 4, borderRadius: 999, backgroundColor: COLORS.border }}><div style={{ height: "100%", width: `${progress * 100}%`, borderRadius: 999, backgroundColor: COLORS.accent }} /></div><div style={{ position: "absolute", right: 58, bottom: 31, color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 11, fontWeight: 700 }}>Q07 · sizeof/strlen · 3:46</div></AbsoluteFill>; };
export const SizeofStrlenQ7Composition = () => <Composition id="SizeofStrlenQ7" component={SizeofStrlenQ7Video} durationInFrames={TOTAL_FRAMES} fps={FPS} width={1920} height={1080} />;
