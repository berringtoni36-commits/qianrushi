import { AbsoluteFill, Composition, Easing, interpolate, Sequence, useCurrentFrame } from "remotion";
import type { CSSProperties, ReactNode } from "react";

const FPS = 30;
const seconds = (value: number) => Math.round(value * FPS);
const COLORS = { background: "#eef2f4", surface: "rgba(255, 255, 255, 0.72)", strong: "rgba(255, 255, 255, 0.90)", text: "#100f0f", muted: "#6f6e69", faint: "#9b9a97", border: "rgba(22, 30, 38, 0.11)", accent: "#d87757", active: "#087f8c", result: "#198754", danger: "#c2413b", compare: "#b45309", violet: "#6f65b8" };
const BODY_FONT = 'Inter, "PingFang SC", "Noto Sans SC", system-ui, sans-serif';
const DISPLAY_FONT = 'Iowan Old Style, "Songti SC", STSong, Georgia, serif';
const CODE_FONT = '"Fira Code", "SFMono-Regular", Menlo, Consolas, monospace';
const ease = Easing.bezier(0.22, 1, 0.36, 1);
const SCENE = { opening: seconds(18), map: seconds(22), memory: seconds(55), decay: seconds(40), traps: seconds(32), followups: seconds(48), final: seconds(34) } as const;
const TOTAL_FRAMES = Object.values(SCENE).reduce((sum, value) => sum + value, 0);
const clamp = (value: number, start: number, end: number) => interpolate(value, [start, end], [0, 1], { extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: ease });
const reveal = (frame: number, start = 0, distance = 16): CSSProperties => { const immediate = start <= 0; const from = immediate ? 0 : start; return { opacity: interpolate(frame, [from, from + 24], immediate ? [0.9, 1] : [0, 1], { extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: ease }), transform: `translateY(${interpolate(frame, [from, from + 24], immediate ? [3, 0] : [distance, 0], { extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: ease })}px)` }; };
const Card: React.FC<{ children: ReactNode; style?: CSSProperties; strong?: boolean }> = ({ children, style, strong }) => <div style={{ border: `1px solid ${COLORS.border}`, borderRadius: 8, backgroundColor: strong ? COLORS.strong : COLORS.surface, boxShadow: "0 16px 48px rgba(22, 30, 38, 0.10)", ...style }}>{children}</div>;
const Background: React.FC = () => <><div style={{ position: "absolute", inset: 0, backgroundColor: COLORS.background, backgroundImage: "linear-gradient(rgba(8, 127, 140, 0.035) 1px, transparent 1px), linear-gradient(90deg, rgba(8, 127, 140, 0.035) 1px, transparent 1px), linear-gradient(135deg, rgba(255,255,255,0.42), rgba(238,242,244,0.18))", backgroundSize: "48px 48px, 48px 48px, 100% 100%" }} /><div style={{ position: "absolute", left: -180, top: 80, width: 760, height: 760, borderRadius: 180, backgroundColor: "rgba(255, 255, 255, 0.28)", transform: "rotate(-16deg)", filter: "blur(12px)" }} /><div style={{ position: "absolute", right: -240, bottom: -260, width: 760, height: 680, borderRadius: 220, backgroundColor: "rgba(216, 119, 87, 0.075)", transform: "rotate(18deg)", filter: "blur(16px)" }} /></>;
const TopBar: React.FC<{ frame: number; section: string; title: string; tag?: string }> = ({ frame, section, title, tag = "慢速复习 · 4:09" }) => <Card strong style={{ position: "absolute", left: 58, right: 58, top: 26, minHeight: 62, padding: "0 20px", display: "flex", alignItems: "center", justifyContent: "space-between", gap: 20, ...reveal(frame) }}><div style={{ display: "flex", alignItems: "center", gap: 14, minWidth: 0 }}><div style={{ color: COLORS.accent, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800 }}>C / C++ 基础</div><div style={{ width: 1, height: 18, backgroundColor: COLORS.border }} /><div style={{ color: COLORS.text, fontSize: 18, fontWeight: 760, whiteSpace: "nowrap" }}>{section}</div><div style={{ color: COLORS.muted, fontSize: 16, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{title}</div></div><div style={{ flex: "0 0 auto", padding: "8px 11px", border: `1px solid ${COLORS.border}`, borderRadius: 6, color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 700 }}>{tag}</div></Card>;
const Caption: React.FC<{ frame: number; label: string; title: string; body: string; color?: string }> = ({ frame, label, title, body, color = COLORS.active }) => <Card strong style={{ position: "absolute", left: 58, right: 58, bottom: 54, minHeight: 106, padding: "15px 20px 16px", borderTop: `3px solid ${color}`, ...reveal(frame) }}><div style={{ display: "flex", justifyContent: "space-between", color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 11, fontWeight: 800 }}><span>{label}</span><span>先理解，再记忆</span></div><div style={{ marginTop: 8, color: COLORS.text, fontSize: 22, fontWeight: 800 }}>{title}</div><div style={{ marginTop: 5, color: COLORS.muted, fontSize: 15, lineHeight: 1.45 }}>{body}</div></Card>;
const SceneTitle: React.FC<{ frame: number; eyebrow: string; title: string; detail: string }> = ({ frame, eyebrow, title, detail }) => <div style={{ position: "absolute", left: 72, right: 72, top: 122, ...reveal(frame) }}><div style={{ color: COLORS.accent, fontFamily: CODE_FONT, fontSize: 14, fontWeight: 800, letterSpacing: 1.3 }}>{eyebrow}</div><div style={{ marginTop: 10, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 44, fontWeight: 700, lineHeight: 1.16 }}>{title}</div><div style={{ marginTop: 9, color: COLORS.muted, fontSize: 18, lineHeight: 1.45 }}>{detail}</div></div>;
const Prompt: React.FC<{ frame: number; question: string; seconds: number; color?: string; hint?: string }> = ({ frame, question, seconds: duration, color = COLORS.active, hint = "先在脑中说一遍，再看标准说法" }) => { const remaining = Math.max(0, Math.ceil((duration * FPS - frame) / FPS)); const progress = clamp(frame, 0, duration * FPS); return <Card strong style={{ width: 670, minHeight: 290, padding: "28px 32px", borderLeft: `4px solid ${color}`, ...reveal(frame, 0, 20) }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800, letterSpacing: 1.2 }}>先暂停 · 主动回忆</div><div style={{ marginTop: 20, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 33, fontWeight: 700, lineHeight: 1.28 }}>{question}</div><div style={{ display: "flex", alignItems: "center", gap: 18, marginTop: 26 }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 44, fontWeight: 800, minWidth: 54 }}>{remaining}</div><div style={{ color: COLORS.muted, fontSize: 16, lineHeight: 1.45 }}>{hint}</div></div><div style={{ height: 6, marginTop: 24, borderRadius: 999, backgroundColor: COLORS.border }}><div style={{ height: "100%", width: `${progress * 100}%`, borderRadius: 999, backgroundColor: color }} /></div></Card>; };
const CodePanel: React.FC<{ title: string; lines: string[]; activeLine?: number; style?: CSSProperties }> = ({ title, lines, activeLine = -1, style }) => <Card strong style={{ overflow: "hidden", backgroundColor: "rgba(247,248,249,0.92)", ...style }}><div style={{ height: 44, display: "flex", alignItems: "center", gap: 8, padding: "0 16px", borderBottom: `1px solid ${COLORS.border}`, color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 750 }}><span style={{ width: 8, height: 8, borderRadius: 999, backgroundColor: COLORS.danger }} /><span style={{ width: 8, height: 8, borderRadius: 999, backgroundColor: COLORS.compare }} /><span style={{ width: 8, height: 8, borderRadius: 999, backgroundColor: COLORS.result }} /><span style={{ marginLeft: 8 }}>{title}</span></div><div style={{ padding: "18px 18px", fontFamily: CODE_FONT, fontSize: 20, lineHeight: 1.75 }}>{lines.map((line, index) => { const active = index === activeLine; return <div key={`${title}-${index}`} style={{ display: "flex", minHeight: 34, padding: "0 10px", borderRadius: 5, backgroundColor: active ? `${COLORS.compare}16` : "transparent", color: active ? COLORS.text : index === 0 ? COLORS.muted : COLORS.text, opacity: activeLine >= 0 && index > activeLine + 2 ? 0.44 : 1 }}><span style={{ width: 31, color: active ? COLORS.compare : COLORS.faint, fontSize: 14 }}>{String(index + 1).padStart(2, "0")}</span><span style={{ whiteSpace: "pre" }}>{line}</span></div>; })}</div></Card>;
const Badge: React.FC<{ children: ReactNode; color: string }> = ({ children, color }) => <div style={{ display: "inline-flex", alignItems: "center", gap: 8, padding: "8px 11px", border: `1px solid ${color}55`, borderRadius: 6, backgroundColor: `${color}12`, color, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 750 }}><span style={{ width: 7, height: 7, borderRadius: 999, backgroundColor: color }} />{children}</div>;

const Opening: React.FC = () => { const frame = useCurrentFrame(); const done = frame >= seconds(6); const cards = [["01", "内存块 vs 地址", "数组有自己的连续空间", COLORS.accent], ["02", "sizeof 与退化", "同一个名字在不同上下文含义不同", COLORS.active], ["03", "传参边界", "缓冲区要和长度一起传", COLORS.result]] as const; return <AbsoluteFill><TopBar frame={frame} section="第 5 题" title="指针和数组有什么区别？" /><SceneTitle frame={frame} eyebrow="复习目标" title="先问：这里存的是一块空间，还是一个地址？" detail="数组和指针经常一起出现，但它们的本质、sizeof 结果和赋值规则完全不同。" /><div style={{ position: "absolute", left: 72, top: 304, right: 72, display: "flex", gap: 26, alignItems: "stretch" }}><Prompt frame={frame} question="指针和数组，最核心的区别是什么？" seconds={6} color={COLORS.active} hint="先说：谁拥有空间、sizeof 看什么、能不能改指向" /><Card strong style={{ flex: 1, padding: "28px 30px", opacity: done ? 1 : 0.42, transform: `translateX(${done ? 0 : 18}px)` }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800, letterSpacing: 1.2 }}>本题路线</div><div style={{ marginTop: 22, display: "grid", gap: 16 }}>{cards.map(([number, label, detail, color]) => <div key={number} style={{ display: "grid", gridTemplateColumns: "44px 1fr", gap: 12, alignItems: "center", padding: "12px 14px", border: `1px solid ${COLORS.border}`, borderRadius: 8, backgroundColor: `${color}0b` }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 15, fontWeight: 800 }}>{number}</div><div><div style={{ color: COLORS.text, fontSize: 20, fontWeight: 800 }}>{label}</div><div style={{ marginTop: 4, color: COLORS.muted, fontSize: 14 }}>{detail}</div></div></div>)}</div></Card></div><Caption frame={frame} label="本题总纲" title="数组是一段连续内存，指针是保存地址的变量" body="数组名会在很多表达式中退化成首元素指针，但退化不等于两者本质相同。" /></AbsoluteFill>; };

const MAP = [["数组", "int arr[5];", "自己拥有连续的 5 个 int 空间", COLORS.accent], ["指针", "int *p = arr;", "变量本身只保存一个地址", COLORS.active], ["参数", "void func(int buf[])", "函数参数里的数组会退化为指针", COLORS.result]] as const;
const MapScene: React.FC = () => { const frame = useCurrentFrame(); return <AbsoluteFill><TopBar frame={frame} section="第 5 题" title="数组、指针和函数参数" /><SceneTitle frame={frame} eyebrow="答案地图" title="先看它拥有的东西，再看它如何被使用" detail="数组拥有一段空间；指针拥有一个可改变的地址值；参数声明中的数组会被调整成指针。" /><div style={{ position: "absolute", left: 72, right: 72, top: 306, display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 18 }}>{MAP.map(([title, code, detail, color], index) => <Card key={title} strong style={{ minHeight: 290, padding: "26px 24px", borderTop: `4px solid ${color}`, ...reveal(frame, index * 26, 22) }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>{String(index + 1).padStart(2, "0")}</div><div style={{ marginTop: 22, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 31, fontWeight: 700 }}>{title}</div><div style={{ marginTop: 28, padding: "12px 14px", borderRadius: 6, backgroundColor: `${color}10`, color, fontFamily: CODE_FONT, fontSize: 16 }}>{code}</div><div style={{ marginTop: 16, color: COLORS.muted, fontSize: 16, lineHeight: 1.45 }}>{detail}</div></Card>)}</div><Caption frame={frame} label="答案框架" title="退化是上下文行为，不是数组变成了指针变量" body="接下来用同一组 arr 和 p，观察空间、地址、sizeof 和赋值规则如何分开。" /></AbsoluteFill>; };

const MEMORY_STATES = [["数组创建", "arr[0..4]", "连续空间属于 arr", COLORS.accent], ["指针指向", "p = &arr[0]", "p 保存首元素地址", COLORS.active], ["指针移动", "p = &arr[2]", "p 可以改变指向，arr 仍然是原来的数组", COLORS.compare], ["指向别处", "p = other", "p 可以改指向，数组不能整体重新赋值", COLORS.result]] as const;
const MemoryScene: React.FC = () => { const frame = useCurrentFrame(); const promptDone = frame >= seconds(7); const phase = promptDone ? Math.min(3, Math.floor((frame - seconds(7)) / seconds(10))) : -1; const activeLine = phase < 0 ? 0 : phase === 0 ? 0 : phase === 1 ? 1 : phase === 2 ? 2 : 3; return <AbsoluteFill><TopBar frame={frame} section="第 5 题 / 内存" title="数组拥有空间，指针保存地址" /><SceneTitle frame={frame} eyebrow="确定性轨迹" title="同一段代码里，arr 和 p 的身份始终不同" detail="arr 是固定的连续内存块；p 是可以改写的地址变量，指向可以随着赋值变化。" /><div style={{ position: "absolute", left: 72, right: 72, top: 300, height: 580, display: "grid", gridTemplateColumns: "0.9fr 1.1fr", gap: 22 }}><CodePanel title="array_pointer.c" activeLine={activeLine} style={{ height: 500 }} lines={["int arr[5] = {0};", "int *p = &arr[0];", "p = &arr[2];", "p = other;", "// arr 不能整体赋值"]} /><Card strong style={{ padding: "24px 24px", position: "relative" }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>内存关系 · 当前阶段 {phase < 0 ? "等待回忆" : `${phase + 1} / 4`}</div><div style={{ marginTop: 18, display: "grid", gap: 14 }}>{MEMORY_STATES.map(([title, code, detail, color], index) => { const active = index === phase; return <div key={title} style={{ padding: "15px 16px", border: `1px solid ${active ? color : COLORS.border}`, borderTop: `3px solid ${color}`, borderRadius: 7, backgroundColor: active ? `${color}12` : "rgba(255,255,255,0.52)", ...reveal(frame, 18 + index * 20) }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 14 }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 17, fontWeight: 800 }}>{title}</div><Badge color={color}>{active ? "当前" : "关系"}</Badge></div><div style={{ marginTop: 10, color: COLORS.text, fontFamily: CODE_FONT, fontSize: 15 }}>{code}</div><div style={{ marginTop: 8, color: COLORS.muted, fontSize: 15, lineHeight: 1.4 }}>{detail}</div></div>; })}</div>{!promptDone && <div style={{ position: "absolute", inset: 22, display: "flex", alignItems: "center", justifyContent: "center", backgroundColor: "rgba(255,255,255,0.74)", borderRadius: 8 }}><Prompt frame={frame} question="arr 和 p，谁能改指向？谁拥有空间？" seconds={7} color={COLORS.active} hint="先说：数组本体、地址变量、整体赋值" /></div>}</Card></div><Caption frame={frame} label="内存口诀" title="数组名不是可赋值的指针变量" body="在表达式里 arr 常常提供首元素地址，但 arr 自己不能像 p 一样改成指向另一块空间。" /></AbsoluteFill>; };

const DecayScene: React.FC = () => { const frame = useCurrentFrame(); const promptDone = frame >= seconds(6); const active = promptDone ? Math.min(2, Math.floor((frame - seconds(6)) / seconds(9))) : -1; return <AbsoluteFill><TopBar frame={frame} section="第 5 题 / 退化" title="函数传参：数组为什么变成指针？" /><SceneTitle frame={frame} eyebrow="高频陷阱" title="函数参数里的 int buf[]，本质上按 int * 处理" detail="数组不能按值传递；函数接收到的是首元素地址，所以 sizeof(buf) 不再是原数组大小。" /><div style={{ position: "absolute", left: 72, right: 72, top: 302, height: 560, display: "grid", gridTemplateColumns: "0.95fr 1.05fr", gap: 22 }}><CodePanel title="function.c" activeLine={active < 0 ? 0 : active + 1} style={{ height: 500 }} lines={["int arr[5];", "sizeof(arr) = 5 * sizeof(int);", "void func(int buf[])", "sizeof(buf) = sizeof(int *);", "void func2(int *buf, int len);"]} /><Card strong style={{ padding: "24px 24px", position: "relative" }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>参数视角 · 当前阶段 {active < 0 ? "等待回忆" : `${active + 1} / 3`}</div><div style={{ marginTop: 18, display: "grid", gap: 15 }}>{[["调用前", "arr 拥有 5 个元素的连续空间", COLORS.accent], ["进入 func", "buf 接收首元素地址，数组参数已调整为指针", COLORS.active], ["安全接口", "指针和长度一起传，函数才知道缓冲区边界", COLORS.result]].map(([title, detail, color], index) => { const selected = index === active; return <div key={title} style={{ padding: "17px 18px", borderLeft: `4px solid ${color}`, border: `1px solid ${selected ? color : COLORS.border}`, borderLeftWidth: 4, borderRadius: 7, backgroundColor: selected ? `${color}11` : "rgba(255,255,255,0.50)", ...reveal(frame, 18 + index * 18) }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}><div style={{ color: COLORS.text, fontSize: 20, fontWeight: 800 }}>{title}</div><Badge color={color}>{selected ? "当前" : "阶段"}</Badge></div><div style={{ marginTop: 9, color: COLORS.muted, fontSize: 15, lineHeight: 1.45 }}>{detail}</div></div>; })}</div>{!promptDone && <div style={{ position: "absolute", inset: 22, display: "flex", alignItems: "center", justifyContent: "center", backgroundColor: "rgba(255,255,255,0.74)", borderRadius: 8 }}><Prompt frame={frame} question="func(int buf[]) 里 sizeof(buf) 还是整个数组吗？" seconds={6} color={COLORS.accent} hint="先判断参数调整和原数组边界" /></div>}</Card></div><Caption frame={frame} label="退化总结" title="传缓冲区时，指针和长度要成对出现" body="sizeof(arr) 只能在数组仍处于数组类型的作用域里得到完整大小；进入参数后不要再猜长度。" color={COLORS.result} /></AbsoluteFill>; };

const TrapsScene: React.FC = () => { const frame = useCurrentFrame(); const leftActive = frame >= seconds(6); const rightActive = frame >= seconds(14); return <AbsoluteFill><TopBar frame={frame} section="第 5 题 / 对比" title="sizeof、赋值和指针数组不要混答" /><SceneTitle frame={frame} eyebrow="易错点" title="看起来都像地址，规则却不是一套" detail="数组大小、指针大小、整体赋值和声明括号，都会在面试里被单独追问。" /><div style={{ position: "absolute", left: 72, right: 72, top: 306, display: "grid", gridTemplateColumns: "1fr 1fr", gap: 18 }}><Card strong style={{ minHeight: 490, padding: "26px 24px", borderTop: `4px solid ${COLORS.active}`, ...reveal(frame, 0) }}><div style={{ color: COLORS.active, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>sizeof 与赋值</div><div style={{ marginTop: 20, display: "grid", gap: 12 }}>{["sizeof(arr) = 5 * sizeof(int)", "sizeof(p) = sizeof(int *)，与平台指针宽度有关", "arr = other; 非法，p = other; 合法"].map((line, index) => <div key={line} style={{ padding: "13px 14px", border: `1px solid ${COLORS.border}`, borderRadius: 6, color: index === 2 ? COLORS.compare : COLORS.text, fontSize: 17, fontWeight: index === 2 ? 700 : 600, opacity: leftActive ? 1 : 0.42 }}>{line}</div>)}</div></Card><Card strong style={{ minHeight: 490, padding: "26px 24px", borderTop: `4px solid ${COLORS.compare}`, ...reveal(frame, 18) }}><div style={{ color: COLORS.compare, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>声明括号决定含义</div><div style={{ marginTop: 20, display: "grid", gap: 12 }}>{["int *p[5]：5 个 int*，指针数组", "int (*p)[5]：指向含 5 个 int 的数组", "数组名常退化为首元素指针，但不能被赋值"].map((line) => <div key={line} style={{ padding: "13px 14px", border: `1px solid ${COLORS.border}`, borderRadius: 6, color: COLORS.text, fontFamily: line.includes("int") ? CODE_FONT : BODY_FONT, fontSize: 16, fontWeight: 650, opacity: rightActive ? 1 : 0.42 }}>{line}</div>)}</div><div style={{ marginTop: 22, padding: "13px 14px", borderRadius: 6, backgroundColor: `${COLORS.compare}12`, color: COLORS.compare, fontSize: 16, fontWeight: 750 }}>先读声明，再判断是数组、指针还是指向数组的指针。</div></Card></div><Caption frame={frame} label="对比总结" title="数组退化只改变表达式中的使用方式，不改变数组本体" body="面试回答按‘本质、sizeof、赋值、传参’四个角度展开，最不容易漏点。" color={COLORS.compare} /></AbsoluteFill>; };

const FOLLOWUPS = [["数组名是常量指针吗？", "可以帮助理解它不能被赋值，但更准确地说，数组名在大多数表达式中会调整为首元素指针。", COLORS.active], ["函数参数中数组为什么退化？", "C 语言数组不能按值传递，参数声明会调整为指针，因此函数里拿不到原数组大小。", COLORS.accent], ["指针数组和数组指针区别？", "int *p[5] 是 5 个 int*；int (*p)[5] 是指向含 5 个 int 的数组。", COLORS.result], ["嵌入式如何安全传缓冲区？", "用指针加长度一起传，例如 func(buf, sizeof(buf))，不要在函数内猜数组大小。", COLORS.violet]] as const;
const FollowupScene: React.FC = () => { const frame = useCurrentFrame(); const index = Math.min(3, Math.floor(frame / seconds(12))); const local = frame - index * seconds(12); const answer = local >= seconds(5); const [question, explanation, color] = FOLLOWUPS[index]; return <AbsoluteFill><TopBar frame={frame} section="第 5 题 / 追问" title="面试官常问的四个边界问题" tag="慢速复习 · 0:48" /><SceneTitle frame={frame} eyebrow={`追问 ${index + 1} / 4`} title="先回答，再看标准说法" detail="每个追问先留 5 秒，答案只保留面试中真正需要说清的边界。" /><div style={{ position: "absolute", left: 72, right: 72, top: 302, display: "grid", gridTemplateColumns: "1.55fr 0.95fr", gap: 22 }}><Card strong style={{ minHeight: 570, padding: "34px 38px", borderLeft: `4px solid ${color}`, ...reveal(frame) }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800, letterSpacing: 1.2 }}>INTERVIEW FOLLOW-UP</div><div style={{ marginTop: 26, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 35, fontWeight: 700, lineHeight: 1.28 }}>{question}</div>{!answer ? <><div style={{ display: "flex", alignItems: "center", gap: 18, marginTop: 30 }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 46, fontWeight: 800 }}>{Math.max(0, Math.ceil((seconds(5) - local) / FPS))}</div><div style={{ color: COLORS.muted, fontSize: 17 }}>不要只说一个关键词，补上为什么。</div></div><div style={{ height: 6, marginTop: 24, borderRadius: 999, backgroundColor: COLORS.border }}><div style={{ height: "100%", width: `${clamp(local, 0, seconds(5)) * 100}%`, backgroundColor: color, borderRadius: 999 }} /></div></> : <div style={{ marginTop: 34, padding: "20px 22px", border: `1px solid ${color}44`, borderRadius: 7, backgroundColor: `${color}10`, color: COLORS.text, fontSize: 21, fontWeight: 750, lineHeight: 1.55, ...reveal(local, seconds(5)) }}>{explanation}</div>}</Card><Card strong style={{ minHeight: 570, padding: "26px 24px" }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>REVIEW QUEUE</div><div style={{ marginTop: 18, display: "grid", gap: 12 }}>{FOLLOWUPS.map(([item], itemIndex) => <div key={item} style={{ padding: "14px 14px", border: `1px solid ${itemIndex === index ? color : COLORS.border}`, borderRadius: 7, backgroundColor: itemIndex === index ? `${color}10` : "rgba(255,255,255,0.48)", opacity: itemIndex > index ? 0.58 : 1 }}><div style={{ color: itemIndex === index ? color : COLORS.faint, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>{String(itemIndex + 1).padStart(2, "0")}</div><div style={{ marginTop: 7, color: COLORS.text, fontSize: 16, lineHeight: 1.4 }}>{item}</div></div>)}</div></Card></div><Caption frame={frame} label="面试追问 · 复述" title="先说数组/指针本质，再补 sizeof 和传参边界" body="把‘退化’说成上下文行为，而不是数组被永久变成指针，回答会更准确。" color={color} /></AbsoluteFill>; };

const FINAL_ITEMS = [["数组和指针本质？", "数组是一段连续空间，指针是保存地址的变量。", COLORS.accent], ["sizeof 为什么不同？", "数组得到整体大小，指针得到指针变量大小，具体宽度看目标平台。", COLORS.active], ["函数参数要注意什么？", "数组参数会调整为指针，函数需要额外接收长度。", COLORS.result], ["最后一个陷阱？", "int *p[5] 是指针数组，int (*p)[5] 是数组指针。", COLORS.danger]] as const;
const FinalScene: React.FC = () => { const frame = useCurrentFrame(); const unit = seconds(8.5); const index = Math.min(3, Math.floor(frame / unit)); const local = frame - index * unit; const answer = local >= seconds(5.5); const [question, answerText, color] = FINAL_ITEMS[index]; return <AbsoluteFill><TopBar frame={frame} section="第 5 题 / 收束" title="最后一次：不看答案，自己复述" tag="慢速复习 · 0:34" /><SceneTitle frame={frame} eyebrow="记忆收束" title="把数组和指针压缩成四个可复述的句子" detail="真正复习完成的标志，是你能把本质、sizeof、退化和声明括号分开说清楚。" /><div style={{ position: "absolute", left: 72, right: 72, top: 306, display: "grid", gridTemplateColumns: "1.1fr 0.9fr", gap: 22 }}><Card strong style={{ minHeight: 565, padding: "32px 38px", borderLeft: `4px solid ${color}` }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800 }}>FINAL RECALL · {index + 1}/4</div><div style={{ marginTop: 26, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 35, fontWeight: 700, lineHeight: 1.28 }}>{question}</div>{!answer ? <div style={{ marginTop: 28, color: COLORS.muted, fontSize: 17 }}>先停 5.5 秒，再继续。</div> : <div style={{ marginTop: 32, padding: "18px 20px", border: `1px solid ${color}44`, borderRadius: 7, backgroundColor: `${color}10`, color: COLORS.text, fontSize: 21, fontWeight: 750, lineHeight: 1.55, ...reveal(local, seconds(5.5)) }}>{answerText}</div>}</Card><Card strong style={{ minHeight: 565, padding: "26px 24px" }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>ONE-LINE SUMMARY</div><div style={{ marginTop: 28, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 34, fontWeight: 700 }}>array = 连续空间</div><div style={{ marginTop: 22, display: "grid", gap: 12 }}>{[["本质", "数组有空间，指针存地址"], ["sizeof", "数组整体，指针变量"], ["传参", "指针 + 长度"]].map(([label, text], itemIndex) => <div key={label} style={{ padding: "15px 16px", border: `1px solid ${itemIndex === index ? color : COLORS.border}`, borderRadius: 7, backgroundColor: itemIndex === index ? `${color}10` : "rgba(255,255,255,0.48)" }}><div style={{ color: itemIndex === index ? color : COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>{label}</div><div style={{ marginTop: 6, color: COLORS.text, fontSize: 17, fontWeight: 700 }}>{text}</div></div>)}</div></Card></div><Caption frame={frame} label="复习完成" title="能说清这四句，第 5 题就真正过了一遍" body="下次复习先问‘这里是数组还是地址’，再补 sizeof 和函数参数的退化边界。" color={COLORS.result} /></AbsoluteFill>; };

export const ArrayPointerQ5Video: React.FC = () => { const frame = useCurrentFrame(); const progress = interpolate(frame, [0, TOTAL_FRAMES - 1], [0, 1], { extrapolateLeft: "clamp", extrapolateRight: "clamp" }); const starts = { opening: 0, map: SCENE.opening, memory: SCENE.opening + SCENE.map, decay: SCENE.opening + SCENE.map + SCENE.memory, traps: SCENE.opening + SCENE.map + SCENE.memory + SCENE.decay, followups: SCENE.opening + SCENE.map + SCENE.memory + SCENE.decay + SCENE.traps, final: SCENE.opening + SCENE.map + SCENE.memory + SCENE.decay + SCENE.traps + SCENE.followups }; return <AbsoluteFill style={{ fontFamily: BODY_FONT, color: COLORS.text, overflow: "hidden" }}><Background /><Sequence from={starts.opening} durationInFrames={SCENE.opening}><Opening /></Sequence><Sequence from={starts.map} durationInFrames={SCENE.map}><MapScene /></Sequence><Sequence from={starts.memory} durationInFrames={SCENE.memory}><MemoryScene /></Sequence><Sequence from={starts.decay} durationInFrames={SCENE.decay}><DecayScene /></Sequence><Sequence from={starts.traps} durationInFrames={SCENE.traps}><TrapsScene /></Sequence><Sequence from={starts.followups} durationInFrames={SCENE.followups}><FollowupScene /></Sequence><Sequence from={starts.final} durationInFrames={SCENE.final}><FinalScene /></Sequence><div style={{ position: "absolute", left: 58, right: 58, bottom: 22, height: 4, borderRadius: 999, backgroundColor: COLORS.border }}><div style={{ height: "100%", width: `${progress * 100}%`, borderRadius: 999, backgroundColor: COLORS.accent }} /></div><div style={{ position: "absolute", right: 58, bottom: 31, color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 11, fontWeight: 700 }}>Q05 · array/pointer · 4:09</div></AbsoluteFill>; };
export const ArrayPointerQ5Composition = () => <Composition id="ArrayPointerQ5" component={ArrayPointerQ5Video} durationInFrames={TOTAL_FRAMES} fps={FPS} width={1920} height={1080} />;
