import { AbsoluteFill, Composition, Easing, interpolate, Sequence, useCurrentFrame } from "remotion";
import type { CSSProperties, ReactNode } from "react";

const FPS = 30;
const seconds = (value: number) => Math.round(value * FPS);
const COLORS = { background: "#eef2f4", surface: "rgba(255, 255, 255, 0.72)", strong: "rgba(255, 255, 255, 0.90)", text: "#100f0f", muted: "#6f6e69", faint: "#9b9a97", border: "rgba(22, 30, 38, 0.11)", accent: "#d87757", active: "#087f8c", result: "#198754", danger: "#c2413b", compare: "#b45309", violet: "#6f65b8" };
const BODY_FONT = 'Inter, "PingFang SC", "Noto Sans SC", system-ui, sans-serif';
const DISPLAY_FONT = 'Iowan Old Style, "Songti SC", STSong, Georgia, serif';
const CODE_FONT = '"Fira Code", "SFMono-Regular", Menlo, Consolas, monospace';
const ease = Easing.bezier(0.22, 1, 0.36, 1);
const SCENE = { opening: seconds(18), map: seconds(20), binding: seconds(48), code: seconds(46), choice: seconds(32), followups: seconds(48), final: seconds(26) } as const;
const TOTAL_FRAMES = Object.values(SCENE).reduce((sum, value) => sum + value, 0);
const clamp = (value: number, start: number, end: number) => interpolate(value, [start, end], [0, 1], { extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: ease });
const reveal = (frame: number, start = 0, distance = 16): CSSProperties => { const immediate = start <= 0; const from = immediate ? 0 : start; return { opacity: interpolate(frame, [from, from + 24], immediate ? [0.9, 1] : [0, 1], { extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: ease }), transform: `translateY(${interpolate(frame, [from, from + 24], immediate ? [3, 0] : [distance, 0], { extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: ease })}px)` }; };
const Card: React.FC<{ children: ReactNode; style?: CSSProperties; strong?: boolean }> = ({ children, style, strong }) => <div style={{ border: `1px solid ${COLORS.border}`, borderRadius: 8, backgroundColor: strong ? COLORS.strong : COLORS.surface, boxShadow: "0 16px 48px rgba(22, 30, 38, 0.10)", ...style }}>{children}</div>;
const Background: React.FC = () => <><div style={{ position: "absolute", inset: 0, backgroundColor: COLORS.background, backgroundImage: "linear-gradient(rgba(8, 127, 140, 0.035) 1px, transparent 1px), linear-gradient(90deg, rgba(8, 127, 140, 0.035) 1px, transparent 1px), linear-gradient(135deg, rgba(255,255,255,0.42), rgba(238,242,244,0.18))", backgroundSize: "48px 48px, 48px 48px, 100% 100%" }} /><div style={{ position: "absolute", left: -180, top: 80, width: 760, height: 760, borderRadius: 180, backgroundColor: "rgba(255, 255, 255, 0.28)", transform: "rotate(-16deg)", filter: "blur(12px)" }} /><div style={{ position: "absolute", right: -240, bottom: -260, width: 760, height: 680, borderRadius: 220, backgroundColor: "rgba(216, 119, 87, 0.075)", transform: "rotate(18deg)", filter: "blur(16px)" }} /></>;
const TopBar: React.FC<{ frame: number; section: string; title: string; tag?: string }> = ({ frame, section, title, tag = "慢速复习 · 3:58" }) => <Card strong style={{ position: "absolute", left: 58, right: 58, top: 26, minHeight: 62, padding: "0 20px", display: "flex", alignItems: "center", justifyContent: "space-between", gap: 20, ...reveal(frame) }}><div style={{ display: "flex", alignItems: "center", gap: 14, minWidth: 0 }}><div style={{ color: COLORS.accent, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800 }}>C / C++ 基础</div><div style={{ width: 1, height: 18, backgroundColor: COLORS.border }} /><div style={{ color: COLORS.text, fontSize: 18, fontWeight: 760, whiteSpace: "nowrap" }}>{section}</div><div style={{ color: COLORS.muted, fontSize: 16, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{title}</div></div><div style={{ flex: "0 0 auto", padding: "8px 11px", border: `1px solid ${COLORS.border}`, borderRadius: 6, color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 700 }}>{tag}</div></Card>;
const Caption: React.FC<{ frame: number; label: string; title: string; body: string; color?: string }> = ({ frame, label, title, body, color = COLORS.active }) => <Card strong style={{ position: "absolute", left: 58, right: 58, bottom: 54, minHeight: 106, padding: "15px 20px 16px", borderTop: `3px solid ${color}`, ...reveal(frame) }}><div style={{ display: "flex", justifyContent: "space-between", color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 11, fontWeight: 800 }}><span>{label}</span><span>先理解，再记忆</span></div><div style={{ marginTop: 8, color: COLORS.text, fontSize: 22, fontWeight: 800 }}>{title}</div><div style={{ marginTop: 5, color: COLORS.muted, fontSize: 15, lineHeight: 1.45 }}>{body}</div></Card>;
const SceneTitle: React.FC<{ frame: number; eyebrow: string; title: string; detail: string }> = ({ frame, eyebrow, title, detail }) => <div style={{ position: "absolute", left: 72, right: 72, top: 122, ...reveal(frame) }}><div style={{ color: COLORS.accent, fontFamily: CODE_FONT, fontSize: 14, fontWeight: 800, letterSpacing: 1.3 }}>{eyebrow}</div><div style={{ marginTop: 10, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 44, fontWeight: 700, lineHeight: 1.16 }}>{title}</div><div style={{ marginTop: 9, color: COLORS.muted, fontSize: 18, lineHeight: 1.45 }}>{detail}</div></div>;
const Prompt: React.FC<{ frame: number; question: string; seconds: number; color?: string; hint?: string }> = ({ frame, question, seconds: duration, color = COLORS.active, hint = "先在脑中说一遍，再看标准说法" }) => { const remaining = Math.max(0, Math.ceil((duration * FPS - frame) / FPS)); const progress = clamp(frame, 0, duration * FPS); return <Card strong style={{ width: 670, minHeight: 290, padding: "28px 32px", borderLeft: `4px solid ${color}`, ...reveal(frame, 0, 20) }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800, letterSpacing: 1.2 }}>先暂停 · 主动回忆</div><div style={{ marginTop: 20, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 33, fontWeight: 700, lineHeight: 1.28 }}>{question}</div><div style={{ display: "flex", alignItems: "center", gap: 18, marginTop: 26 }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 44, fontWeight: 800, minWidth: 54 }}>{remaining}</div><div style={{ color: COLORS.muted, fontSize: 16, lineHeight: 1.45 }}>{hint}</div></div><div style={{ height: 6, marginTop: 24, borderRadius: 999, backgroundColor: COLORS.border }}><div style={{ height: "100%", width: `${progress * 100}%`, borderRadius: 999, backgroundColor: color }} /></div></Card>; };
const CodePanel: React.FC<{ title: string; lines: string[]; activeLine?: number; style?: CSSProperties }> = ({ title, lines, activeLine = -1, style }) => <Card strong style={{ overflow: "hidden", backgroundColor: "rgba(247,248,249,0.92)", ...style }}><div style={{ height: 44, display: "flex", alignItems: "center", gap: 8, padding: "0 16px", borderBottom: `1px solid ${COLORS.border}`, color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 750 }}><span style={{ width: 8, height: 8, borderRadius: 999, backgroundColor: COLORS.danger }} /><span style={{ width: 8, height: 8, borderRadius: 999, backgroundColor: COLORS.compare }} /><span style={{ width: 8, height: 8, borderRadius: 999, backgroundColor: COLORS.result }} /><span style={{ marginLeft: 8 }}>{title}</span></div><div style={{ padding: "18px 18px", fontFamily: CODE_FONT, fontSize: 20, lineHeight: 1.75 }}>{lines.map((line, index) => { const active = index === activeLine; return <div key={`${title}-${index}`} style={{ display: "flex", minHeight: 34, padding: "0 10px", borderRadius: 5, backgroundColor: active ? `${COLORS.compare}16` : "transparent", color: active ? COLORS.text : index === 0 ? COLORS.muted : COLORS.text, opacity: activeLine >= 0 && index > activeLine + 2 ? 0.44 : 1 }}><span style={{ width: 31, color: active ? COLORS.compare : COLORS.faint, fontSize: 14 }}>{String(index + 1).padStart(2, "0")}</span><span style={{ whiteSpace: "pre" }}>{line}</span></div>; })}</div></Card>;
const Badge: React.FC<{ children: ReactNode; color: string }> = ({ children, color }) => <div style={{ display: "inline-flex", alignItems: "center", gap: 8, padding: "8px 11px", border: `1px solid ${color}55`, borderRadius: 6, backgroundColor: `${color}12`, color, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 750 }}><span style={{ width: 7, height: 7, borderRadius: 999, backgroundColor: color }} />{children}</div>;

const Opening: React.FC = () => { const frame = useCurrentFrame(); const done = frame >= seconds(6); const cards = [["01", "空值语义", "指针可以是 nullptr，引用不能表达空", COLORS.danger], ["02", "绑定关系", "指针能改指向，引用绑定后不更换", COLORS.active], ["03", "接口选择", "看参数是否可空，再决定 T* 还是 T&", COLORS.result]] as const; return <AbsoluteFill><TopBar frame={frame} section="第 6 题" title="指针和引用有什么区别？" /><SceneTitle frame={frame} eyebrow="复习目标" title="先问：这是一个地址变量，还是对象的别名？" detail="两者都能间接访问对象，但空值、初始化、重新绑定和接口语义完全不同。" /><div style={{ position: "absolute", left: 72, top: 304, right: 72, display: "flex", gap: 26, alignItems: "stretch" }}><Prompt frame={frame} question="指针和引用最核心的区别是什么？" seconds={6} color={COLORS.active} hint="先说：能否为空、能否改指向、如何初始化" /><Card strong style={{ flex: 1, padding: "28px 30px", opacity: done ? 1 : 0.42, transform: `translateX(${done ? 0 : 18}px)` }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800, letterSpacing: 1.2 }}>本题路线</div><div style={{ marginTop: 22, display: "grid", gap: 16 }}>{cards.map(([number, label, detail, color]) => <div key={number} style={{ display: "grid", gridTemplateColumns: "44px 1fr", gap: 12, alignItems: "center", padding: "12px 14px", border: `1px solid ${COLORS.border}`, borderRadius: 8, backgroundColor: `${color}0b` }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 15, fontWeight: 800 }}>{number}</div><div><div style={{ color: COLORS.text, fontSize: 20, fontWeight: 800 }}>{label}</div><div style={{ marginTop: 4, color: COLORS.muted, fontSize: 14 }}>{detail}</div></div></div>)}</div></Card></div><Caption frame={frame} label="本题总纲" title="指针是存地址的变量，引用是对象的别名" body="指针可为空、可改指向；引用必须在定义时绑定有效对象，绑定后不再换对象。" /></AbsoluteFill>; };

const MAP = [["指针", "int *p = nullptr;", "有自己的地址值，能表达空和改指向", COLORS.danger], ["引用", "int &r = a;", "不单独表达空，绑定后直接作为对象使用", COLORS.active], ["选择", "T*  /  T&", "参数可空用指针，必须有效且不拷贝可用引用", COLORS.result]] as const;
const MapScene: React.FC = () => { const frame = useCurrentFrame(); return <AbsoluteFill><TopBar frame={frame} section="第 6 题" title="指针、引用和接口语义" /><SceneTitle frame={frame} eyebrow="答案地图" title="先看语言规则，再看工程选择" detail="指针强调地址和可选性；引用强调已存在对象的别名；接口应把这种约束直接表达出来。" /><div style={{ position: "absolute", left: 72, right: 72, top: 306, display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 18 }}>{MAP.map(([title, code, detail, color], index) => <Card key={title} strong style={{ minHeight: 294, padding: "26px 24px", borderTop: `4px solid ${color}`, ...reveal(frame, index * 26, 22) }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>{String(index + 1).padStart(2, "0")}</div><div style={{ marginTop: 22, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 31, fontWeight: 700 }}>{title}</div><div style={{ marginTop: 28, padding: "12px 14px", borderRadius: 6, backgroundColor: `${color}10`, color, fontFamily: CODE_FONT, fontSize: 16 }}>{code}</div><div style={{ marginTop: 16, color: COLORS.muted, fontSize: 16, lineHeight: 1.45 }}>{detail}</div></Card>)}</div><Caption frame={frame} label="答案框架" title="先判断‘可不可以没有对象’，再判断‘需不需要改指向’" body="不要把引用说成‘更小的指针’；那只是常见实现方式，不是语言层面的完整定义。" color={COLORS.compare} /></AbsoluteFill>; };

const BINDING_STATES = [["指针为空", "p = nullptr", "p 没有指向对象，空值是合法状态", COLORS.danger], ["指向 a", "p = &a", "p 保存 a 的地址，需要 *p 才能访问 a", COLORS.active], ["改指向 b", "p = &b", "同一个 p 改为保存 b 的地址", COLORS.compare], ["引用绑定", "int &r = a", "r 在定义时绑定 a，不能声明成空引用", COLORS.accent], ["引用写入", "r = 40", "修改的是 a 的值，不是更换 r 的绑定对象", COLORS.result]] as const;
const BindingScene: React.FC = () => { const frame = useCurrentFrame(); const promptDone = frame >= seconds(7); const phase = promptDone ? Math.min(4, Math.floor((frame - seconds(7)) / seconds(8))) : -1; const activeLine = phase < 0 ? 2 : [2, 3, 4, 5, 6][phase]; return <AbsoluteFill><TopBar frame={frame} section="第 6 题 / 状态" title="可为空和可改指向，是指针的能力" /><SceneTitle frame={frame} eyebrow="确定性轨迹" title="同一段代码里，p 和 r 的状态变化不同" detail="指针先可以为空，再改为指向 a、b；引用从定义开始就绑定 a，后续赋值只修改 a。" /><div style={{ position: "absolute", left: 72, right: 72, top: 300, height: 580, display: "grid", gridTemplateColumns: "0.88fr 1.12fr", gap: 22 }}><CodePanel title="pointer_reference.cpp" activeLine={activeLine} style={{ height: 500 }} lines={["int a = 10, b = 20;", "int *p = nullptr;", "p = &a;", "p = &b;", "int &r = a;", "r = 40;", "// r 不能重新绑定"]} /><Card strong style={{ padding: "24px 24px", position: "relative" }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>绑定关系 · 当前阶段 {phase < 0 ? "等待回忆" : `${phase + 1} / 5`}</div><div style={{ marginTop: 18, display: "grid", gap: 10 }}>{BINDING_STATES.map(([title, code, detail, color], index) => { const active = index === phase; return <div key={title} style={{ padding: "12px 15px", border: `1px solid ${active ? color : COLORS.border}`, borderTop: `3px solid ${color}`, borderRadius: 7, backgroundColor: active ? `${color}12` : "rgba(255,255,255,0.52)", ...reveal(frame, 18 + index * 17) }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 14 }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 16, fontWeight: 800 }}>{title}</div><Badge color={color}>{active ? "当前" : "关系"}</Badge></div><div style={{ marginTop: 7, color: COLORS.text, fontFamily: CODE_FONT, fontSize: 14 }}>{code}</div><div style={{ marginTop: 5, color: COLORS.muted, fontSize: 14, lineHeight: 1.35 }}>{detail}</div></div>; })}</div>{!promptDone && <div style={{ position: "absolute", inset: 22, display: "flex", alignItems: "center", justifyContent: "center", backgroundColor: "rgba(255,255,255,0.74)", borderRadius: 8 }}><Prompt frame={frame} question="p 和 r，谁可以为空？谁可以改绑定对象？" seconds={7} color={COLORS.active} hint="先分清：地址变量、对象别名、赋值动作" /></div>}</Card></div><Caption frame={frame} label="状态口诀" title="p = 地址变量；r = 固定别名" body="看到 `r = b` 时，先问它是赋值还是重新绑定：引用已经绑定 a，这行只会把 b 的值写入 a。" /></AbsoluteFill>; };

const CODE_STATES = [["指针改指向", "p = &b", "p 从 a 切换到 b，变量 p 的地址值改变", COLORS.compare], ["指针解引用写入", "*p = 30", "当前 p 指向 b，所以 b 变成 30", COLORS.active], ["引用直接写入", "r = 40", "r 是 a 的别名，所以 a 变成 40", COLORS.accent], ["看似重新绑定", "r = b", "不是换绑 r，而是把 b 当前值复制给 a", COLORS.danger]] as const;
const CodeScene: React.FC = () => { const frame = useCurrentFrame(); const promptDone = frame >= seconds(6); const phase = promptDone ? Math.min(3, Math.floor((frame - seconds(6)) / seconds(10))) : -1; const activeLine = phase < 0 ? 2 : [2, 3, 5, 6][phase]; return <AbsoluteFill><TopBar frame={frame} section="第 6 题 / 代码" title="赋值语句不能改变引用的绑定" /><SceneTitle frame={frame} eyebrow="代码对比" title="看清 `*p = 30`、`r = 40` 和 `r = b`" detail="指针需要显式解引用；引用可以直接使用，但直接赋值仍然是在修改已绑定的对象。" /><div style={{ position: "absolute", left: 72, right: 72, top: 300, height: 580, display: "grid", gridTemplateColumns: "0.92fr 1.08fr", gap: 22 }}><CodePanel title="assignment.cpp" activeLine={activeLine} style={{ height: 500 }} lines={["int a = 10, b = 20;", "int *p = &a;", "p = &b;", "*p = 30;", "int &r = a;", "r = 40;", "r = b; // a = b，不是换绑"]} /><Card strong style={{ padding: "24px 24px", position: "relative" }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>执行结果 · 当前阶段 {phase < 0 ? "等待回忆" : `${phase + 1} / 4`}</div><div style={{ marginTop: 18, display: "grid", gap: 14 }}>{CODE_STATES.map(([title, code, detail, color], index) => { const active = index === phase; return <div key={title} style={{ padding: "17px 18px", border: `1px solid ${active ? color : COLORS.border}`, borderLeft: `4px solid ${color}`, borderRadius: 7, backgroundColor: active ? `${color}12` : "rgba(255,255,255,0.52)", ...reveal(frame, 18 + index * 20) }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 14 }}><div style={{ color: COLORS.text, fontSize: 20, fontWeight: 800 }}>{title}</div><Badge color={color}>{active ? "当前" : "结果"}</Badge></div><div style={{ marginTop: 9, color, fontFamily: CODE_FONT, fontSize: 15 }}>{code}</div><div style={{ marginTop: 8, color: COLORS.muted, fontSize: 15, lineHeight: 1.42 }}>{detail}</div></div>; })}</div>{!promptDone && <div style={{ position: "absolute", inset: 22, display: "flex", alignItems: "center", justifyContent: "center", backgroundColor: "rgba(255,255,255,0.74)", borderRadius: 8 }}><Prompt frame={frame} question="`r = b` 会让 r 改绑到 b 吗？" seconds={6} color={COLORS.accent} hint="先追踪 r 绑定的对象，再解释赋值" /></div>}</Card></div><Caption frame={frame} label="代码结论" title="引用的赋值是改对象，不是改绑定" body="指针的‘两步’是改地址再解引用；引用直接读写对象，但不能靠普通赋值换绑。" color={COLORS.result} /></AbsoluteFill>; };

const CHOICES = [["避免拷贝且不能为空", "const T&", "调用者必须提供有效对象，接口表达只读借用", COLORS.active], ["参数可能为空", "T*", "nullptr 是明确的可选状态，函数先判断再解引用", COLORS.danger], ["寄存器 / DMA / 地址操作", "volatile T*", "需要按地址访问硬件或外设映射区域", COLORS.compare], ["C++ 运算符 / STL", "T&", "引用让接口保持对象语义，调用处不需要显式解引用", COLORS.result]] as const;
const ChoiceScene: React.FC = () => { const frame = useCurrentFrame(); const promptDone = frame >= seconds(5); const active = promptDone ? Math.min(3, Math.floor((frame - seconds(5)) / seconds(6))) : -1; return <AbsoluteFill><TopBar frame={frame} section="第 6 题 / 选择" title="什么时候用指针，什么时候用引用？" /><SceneTitle frame={frame} eyebrow="工程边界" title="类型不是风格偏好，而是在表达约束" detail="先判断对象是否允许缺席，再判断调用者是否需要改指向或直接操作对象。" /><div style={{ position: "absolute", left: 72, right: 72, top: 302, display: "grid", gridTemplateColumns: "1fr 1fr", gap: 18 }}>{CHOICES.map(([title, code, detail, color], index) => { const selected = index === active; return <Card key={title} strong style={{ minHeight: 225, padding: "24px 24px", borderTop: `4px solid ${color}`, borderColor: selected ? color : COLORS.border, backgroundColor: selected ? `${color}10` : COLORS.strong, ...reveal(frame, index * 14, 18) }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 14 }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800 }}>{String(index + 1).padStart(2, "0")}</div><Badge color={color}>{selected ? "当前选择" : "场景"}</Badge></div><div style={{ marginTop: 16, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 26, fontWeight: 700 }}>{title}</div><div style={{ marginTop: 14, color, fontFamily: CODE_FONT, fontSize: 21, fontWeight: 800 }}>{code}</div><div style={{ marginTop: 10, color: COLORS.muted, fontSize: 15, lineHeight: 1.4 }}>{detail}</div></Card>; })}</div>{!promptDone && <div style={{ position: "absolute", left: 625, top: 368 }}><Prompt frame={frame} question="函数参数可能为空时，应该选 T* 还是 T&？" seconds={5} color={COLORS.danger} hint="接口先表达‘可选’还是‘必须存在’" /></div>}<Caption frame={frame} label="工程选择" title="可空用指针，必须有效且不想拷贝时常用引用" body="底层寄存器、DMA 和裸地址操作仍然需要指针；引用不能替代所有地址语义。" color={COLORS.compare} /></AbsoluteFill>; };

const FOLLOWUPS = [["引用底层怎么实现？", "多数编译器会用指针一类的机制实现，但这是实现方式；语言层面引用仍是对象别名，不能当可空指针使用。", COLORS.active], ["C 为什么没有引用？", "引用是 C++ 提供的语言抽象；C 通常用指针表达同样的间接访问和可选地址。", COLORS.accent], ["C++ 有指针后为什么还需要引用？", "引用把‘必须绑定有效对象、不能换绑’写进接口，调用语法更直接，语义也更清楚。", COLORS.result], ["引用能完全替代指针吗？", "不能。可空参数、改指向、寄存器映射、DMA 和底层地址计算仍然需要指针。", COLORS.violet]] as const;
const FollowupScene: React.FC = () => { const frame = useCurrentFrame(); const index = Math.min(3, Math.floor(frame / seconds(12))); const local = frame - index * seconds(12); const answer = local >= seconds(5); const [question, explanation, color] = FOLLOWUPS[index]; return <AbsoluteFill><TopBar frame={frame} section="第 6 题 / 追问" title="面试官常问的四个边界问题" tag="慢速复习 · 0:48" /><SceneTitle frame={frame} eyebrow={`追问 ${index + 1} / 4`} title="先回答，再看标准说法" detail="每个追问先留 5 秒，答案只保留面试中真正需要说清的边界。" /><div style={{ position: "absolute", left: 72, right: 72, top: 302, display: "grid", gridTemplateColumns: "1.55fr 0.95fr", gap: 22 }}><Card strong style={{ minHeight: 570, padding: "34px 38px", borderLeft: `4px solid ${color}`, ...reveal(frame) }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800, letterSpacing: 1.2 }}>INTERVIEW FOLLOW-UP</div><div style={{ marginTop: 26, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 35, fontWeight: 700, lineHeight: 1.28 }}>{question}</div>{!answer ? <><div style={{ display: "flex", alignItems: "center", gap: 18, marginTop: 30 }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 46, fontWeight: 800 }}>{Math.max(0, Math.ceil((seconds(5) - local) / FPS))}</div><div style={{ color: COLORS.muted, fontSize: 17 }}>不要只说‘更安全’，补上约束为什么存在。</div></div><div style={{ height: 6, marginTop: 24, borderRadius: 999, backgroundColor: COLORS.border }}><div style={{ height: "100%", width: `${clamp(local, 0, seconds(5)) * 100}%`, backgroundColor: color, borderRadius: 999 }} /></div></> : <div style={{ marginTop: 34, padding: "20px 22px", border: `1px solid ${color}44`, borderRadius: 7, backgroundColor: `${color}10`, color: COLORS.text, fontSize: 21, fontWeight: 750, lineHeight: 1.55, ...reveal(local, seconds(5)) }}>{explanation}</div>}</Card><Card strong style={{ minHeight: 570, padding: "26px 24px" }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>REVIEW QUEUE</div><div style={{ marginTop: 18, display: "grid", gap: 12 }}>{FOLLOWUPS.map(([item], itemIndex) => <div key={item} style={{ padding: "14px 14px", border: `1px solid ${itemIndex === index ? color : COLORS.border}`, borderRadius: 7, backgroundColor: itemIndex === index ? `${color}10` : "rgba(255,255,255,0.48)", opacity: itemIndex > index ? 0.58 : 1 }}><div style={{ color: itemIndex === index ? color : COLORS.faint, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>{String(itemIndex + 1).padStart(2, "0")}</div><div style={{ marginTop: 7, color: COLORS.text, fontSize: 16, lineHeight: 1.4 }}>{item}</div></div>)}</div></Card></div><Caption frame={frame} label="面试追问 · 复述" title="先说语言层面的约束，再补实现和工程边界" body="‘引用通常用指针实现’不能替代引用的语言语义；回答要把两层分开。" color={color} /></AbsoluteFill>; };

const FINAL_ITEMS = [["一句话本质？", "指针是存地址的变量，引用是对象的别名。", COLORS.accent], ["空值和改指向？", "指针可以 nullptr、可以改指向；引用必须初始化，绑定后不换绑。", COLORS.active], ["`r = b` 是什么？", "它把 b 的值赋给引用绑定的对象 a，不是让 r 改绑到 b。", COLORS.result], ["如何选型？", "参数可空或要操作地址用指针；必须有效且表达对象借用时常用引用。", COLORS.danger]] as const;
const FinalScene: React.FC = () => { const frame = useCurrentFrame(); const unit = seconds(6.5); const index = Math.min(3, Math.floor(frame / unit)); const local = frame - index * unit; const answer = local >= seconds(4.2); const [question, answerText, color] = FINAL_ITEMS[index]; return <AbsoluteFill><TopBar frame={frame} section="第 6 题 / 收束" title="最后一次：不看答案，自己复述" tag="慢速复习 · 0:26" /><SceneTitle frame={frame} eyebrow="记忆收束" title="把指针和引用压缩成四个可复述的句子" detail="能说清本质、空值、换绑和接口选择，这道题才算真正复习完成。" /><div style={{ position: "absolute", left: 72, right: 72, top: 306, display: "grid", gridTemplateColumns: "1.1fr 0.9fr", gap: 22 }}><Card strong style={{ minHeight: 565, padding: "32px 38px", borderLeft: `4px solid ${color}` }}><div style={{ color, fontFamily: CODE_FONT, fontSize: 13, fontWeight: 800 }}>FINAL RECALL · {index + 1}/4</div><div style={{ marginTop: 26, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 35, fontWeight: 700, lineHeight: 1.28 }}>{question}</div>{!answer ? <div style={{ marginTop: 28, color: COLORS.muted, fontSize: 17 }}>先停 4.2 秒，再继续。</div> : <div style={{ marginTop: 32, padding: "18px 20px", border: `1px solid ${color}44`, borderRadius: 7, backgroundColor: `${color}10`, color: COLORS.text, fontSize: 21, fontWeight: 750, lineHeight: 1.55, ...reveal(local, seconds(4.2)) }}>{answerText}</div>}</Card><Card strong style={{ minHeight: 565, padding: "26px 24px" }}><div style={{ color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>ONE-LINE SUMMARY</div><div style={{ marginTop: 28, color: COLORS.text, fontFamily: DISPLAY_FONT, fontSize: 34, fontWeight: 700 }}>pointer = 地址变量</div><div style={{ marginTop: 22, display: "grid", gap: 12 }}>{[["指针", "可空，可改指向"], ["引用", "必须绑定，不可换绑"], ["选型", "可空用指针，必有对象常用引用"]].map(([label, text], itemIndex) => <div key={label} style={{ padding: "15px 16px", border: `1px solid ${itemIndex === index ? color : COLORS.border}`, borderRadius: 7, backgroundColor: itemIndex === index ? `${color}10` : "rgba(255,255,255,0.48)" }}><div style={{ color: itemIndex === index ? color : COLORS.muted, fontFamily: CODE_FONT, fontSize: 12, fontWeight: 800 }}>{label}</div><div style={{ marginTop: 6, color: COLORS.text, fontSize: 17, fontWeight: 700 }}>{text}</div></div>)}</div></Card></div><Caption frame={frame} label="复习完成" title="能说清这四句，第 6 题就真正过了一遍" body="下次遇到指针和引用，先问‘对象能不能缺席’，再问‘调用者是否需要改指向’。" color={COLORS.result} /></AbsoluteFill>; };

export const PointerReferenceQ6Video: React.FC = () => { const frame = useCurrentFrame(); const progress = interpolate(frame, [0, TOTAL_FRAMES - 1], [0, 1], { extrapolateLeft: "clamp", extrapolateRight: "clamp" }); const starts = { opening: 0, map: SCENE.opening, binding: SCENE.opening + SCENE.map, code: SCENE.opening + SCENE.map + SCENE.binding, choice: SCENE.opening + SCENE.map + SCENE.binding + SCENE.code, followups: SCENE.opening + SCENE.map + SCENE.binding + SCENE.code + SCENE.choice, final: SCENE.opening + SCENE.map + SCENE.binding + SCENE.code + SCENE.choice + SCENE.followups }; return <AbsoluteFill style={{ fontFamily: BODY_FONT, color: COLORS.text, overflow: "hidden" }}><Background /><Sequence from={starts.opening} durationInFrames={SCENE.opening}><Opening /></Sequence><Sequence from={starts.map} durationInFrames={SCENE.map}><MapScene /></Sequence><Sequence from={starts.binding} durationInFrames={SCENE.binding}><BindingScene /></Sequence><Sequence from={starts.code} durationInFrames={SCENE.code}><CodeScene /></Sequence><Sequence from={starts.choice} durationInFrames={SCENE.choice}><ChoiceScene /></Sequence><Sequence from={starts.followups} durationInFrames={SCENE.followups}><FollowupScene /></Sequence><Sequence from={starts.final} durationInFrames={SCENE.final}><FinalScene /></Sequence><div style={{ position: "absolute", left: 58, right: 58, bottom: 22, height: 4, borderRadius: 999, backgroundColor: COLORS.border }}><div style={{ height: "100%", width: `${progress * 100}%`, borderRadius: 999, backgroundColor: COLORS.accent }} /></div><div style={{ position: "absolute", right: 58, bottom: 31, color: COLORS.muted, fontFamily: CODE_FONT, fontSize: 11, fontWeight: 700 }}>Q06 · pointer/reference · 3:58</div></AbsoluteFill>; };
export const PointerReferenceQ6Composition = () => <Composition id="PointerReferenceQ6" component={PointerReferenceQ6Video} durationInFrames={TOTAL_FRAMES} fps={FPS} width={1920} height={1080} />;
