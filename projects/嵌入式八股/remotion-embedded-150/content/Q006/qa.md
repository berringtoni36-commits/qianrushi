# Q006 QA

## 内容与事实

- [x] 核心答案准确区分指针的空值/改指向与引用的初始化/不可换绑。
- [x] `r = b` 明确解释为给 `a` 赋值，而不是重新绑定引用。
- [x] 没有把引用额外占用存储的实现事实说成标准保证。
- [x] `const T&`、可空 `T*`、寄存器/DMA 指针和 STL/运算符引用的场景边界清楚。

## 分镜与状态

- [x] 共 7 个独立场景，帧区间连续覆盖 0-7139。
- [x] 绑定场景的代码高亮、当前阶段和五张关系卡由同一个 `phase` 派生。
- [x] 代码场景的高亮行、变量结果和四个解释卡由同一个 `phase` 派生。
- [x] 四个追问保留 5 秒回忆时间，四个最终复述保留 4.2 秒回忆时间。

## 视觉与节奏

- [x] 开头、答案地图、绑定状态、代码边界、工程选择、追问和最终复述均完成人工抽帧检查。
- [x] 代表帧覆盖场景入口、每个状态切换后一帧、答案首次出现帧和最后一帧；检查集保存在 `out/Q006/review-frames-v1/`。
- [x] 通过缩小预览检查文字层级、状态颜色、代码高亮和底部字幕，没有遮挡、溢出或不可读文本。
- [x] 最终海报 `out/Q006/Q006-poster.png` 已生成并人工检查。

## 成片规格

- 目标：1920x1080，30fps，H.264 MP4，中等题不超过 4:00。
- 实际：238.058667 秒（约 3:58.06），7140 个视频帧，1920x1080，30fps，H.264 视频 + AAC 音频，文件大小 15,935,465 bytes。
- 全帧扫描：7140/7140 帧完成；最低灰度均值 240.1497；`mean < 8` 的黑屏帧为 0，`mean < 32` 的暗帧为 0。

## 结论

结论：无未解决的高优先级问题。2026-08-16 完成渲染、全帧扫描、关键帧与缩小联系表复核，Q006 可以标记为完成。
