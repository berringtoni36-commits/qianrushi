# Q009 分镜

## 场景帧区间

| 场景 | 帧区间 | 时间 | 主要任务 | 进入状态 | 最终状态 |
|---|---:|---:|---|---|---|
| Opening | 0-479 | 0:00-0:16 | 主动回忆与路线预览 | 问题、6 秒倒计时、路线卡降级显示 | 存储期、启动搬运、工程边界三个入口完整可见 |
| Map | 480-1019 | 0:16-0:34 | 建立存储期到段名的答案地图 | 三张卡按顺序进入 | 自动/静态存储期与 `.data/.bss` 边界可读 |
| Storage | 1020-2279 | 0:34-1:16 | 对照变量声明与实际存储映射 | 7 秒回忆，五个符号逐个定位 | 普通局部、全局、文件 static、局部 static 关系清楚 |
| Startup | 2280-3539 | 1:16-1:58 | 展示 Flash、RAM 和启动代码 | 6 秒回忆，四个启动阶段等待揭示 | `.data` 拷贝、`.bss` 清零、`main` 顺序明确 |
| Embedded | 3540-4619 | 1:58-2:34 | 对比工具链和工程边界 | 5 秒回忆，四个边界逐个激活 | static、Flash/RAM、启动成本、栈测量不再混淆 |
| Follow-ups | 4620-6059 | 2:34-3:22 | 练习四个面试追问 | 每题先倒计时 5 秒 | 作用域、存储区、启动动作和 const 平台边界稳定出现 |
| Final | 6060-6839 | 3:22-3:48 | 最终无提示复述 | 每题先停 4.2 秒 | 四个收束答案完成，最后一帧仍可读 |

## 关键帧验收点

- `0`：题目、6 秒倒计时和待揭示路线卡可读。
- `480`：答案地图开始，三张卡分别在本地帧 `0`、`26`、`52` 后进入。
- `1020`：符号表场景入口；本地帧 `0-209` 为 7 秒回忆，之后每 210 帧切换一个变量。
- `1231`、`1441`、`1651`、`1861`、`2071`：五个变量映射稳定区间。
- `2280`：启动场景入口；本地帧 `0-179` 为 6 秒回忆，之后每 270 帧切换一次启动阶段。
- `2461`、`2731`、`3001`、`3271`：Flash 镜像、`.data` 拷贝、`.bss` 清零、进入 `main`。
- `3540`：工程边界入口；本地帧 `0-149` 为 5 秒回忆，之后每约 233 帧激活一个边界卡。
- `4620`、`4770`、`4980`、`5340`、`5700`：追问入口与答案完全展开附近。
- `6060`、`6186`、`6381`、`6576`、`6771`：最终复述入口与四个答案稳定附近。
- `6839`：最后一个答案、总结字幕和进度条仍完整可读。

## 状态一致性

符号表场景的代码高亮、当前变量和五张映射卡由同一个 `phase` 派生；启动场景的代码高亮、Flash/RAM 高亮和步骤列表由同一个 `phase` 派生；工程边界卡的高亮由同一个 `phase` 派生。
