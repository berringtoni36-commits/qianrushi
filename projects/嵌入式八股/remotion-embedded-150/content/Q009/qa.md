# Q009 QA

## 内容与事实

- [x] 核心答案区分自动存储期、静态存储期、作用域/链接属性和具体段名。
- [x] 没有把“局部一定在栈上”或“const 一定在 Flash”说成语言保证。
- [x] `.data`、`.bss`、Flash 初值镜像、RAM 运行时副本和启动顺序表达准确。
- [x] 文件作用域 `static`、局部 `static`、优化寄存器分配和 linker map 边界明确。

## 分镜与状态

- [x] 共 7 个独立场景，帧区间连续覆盖 0-6839。
- [x] 符号表场景的代码高亮、当前变量和五张映射卡由同一个 `phase` 派生。
- [x] 启动场景的代码高亮、Flash/RAM 高亮和步骤列表由同一个 `phase` 派生；已复核 `copy_data()`、`zero_bss()`、`main()` 三个高亮状态。
- [x] 四个追问保留 5 秒回忆时间，四个最终复述保留 4.2 秒回忆时间。

## 视觉与节奏

- [x] 开头、答案地图、符号表、启动轨迹、工程边界、追问和最终复述均完成人工抽帧检查。
- [x] 代表帧覆盖场景入口、每个状态切换后一帧、答案首次出现帧、答案完全展开帧和最后一帧。
- [x] 通过缩小联系表和 1920x1080 全尺寸关键帧检查文字层级、状态颜色、代码高亮和底部字幕，没有遮挡、溢出或不可读文本。
- [x] 最终海报已生成并人工检查。

复核产物：`out/Q009/review-frames-v1/`，包含 51 个入口、状态切换和答案展开关键帧，以及五组场景联系表和一组答案完全展开联系表。

## 成片规格

- 目标：1920x1080，30fps，H.264 MP4，中等题不超过 4:00。
- 实际：视频流 228.000000 秒、6840 帧、1920x1080、30fps、H.264；音频 AAC，48kHz，双声道，228.053333 秒；文件大小 15,910,068 bytes。
- 全帧扫描：6840/6840 帧完成；64x36 灰度均值最低 240.1471（帧 4612，153.733 秒），`mean < 8` 的黑屏帧为 0，`mean < 32` 的暗帧为 0。

## 结论

结论：无未解决的高优先级问题。2026-08-17 完成修复后重渲染、全帧扫描、关键帧与联系表复核，Q009 可以标记为完成。
