# Q011 QA 记录

## 制作状态

- 状态：complete
- 最后更新：2026-08-17
- 目标视频：out/Q011/Q011-delete-array.mp4
- 目标规格：1920×1080 / 30fps / H.264 MP4
- 设计时长：218 秒（3:38）
- 实际视频时长：218.000 秒
- 容器时长：218.048 秒
- 视频帧：6540 / 6540
- 文件大小：15485229 bytes
- 海报帧：6480

## 内容核对

- [x] 原题、核心区别和原文追问已提取。
- [x] delete 与 delete[] 的适用场景已分开。
- [x] 单对象场景显示 1 次构造、1 次析构和释放后的指针处理。
- [x] 数组场景显示 3 个 live objects，并显示逐个、逆序析构。
- [x] 错误配对使用“未定义行为”，没有承诺固定的错误结果。
- [x] delete 后悬空指针、p = nullptr 和 delete nullptr 已区分。
- [x] 嵌入式边界覆盖静态分配、固定块、资源所有权和失败路径。

## 代码与结构核对

- [x] 所有动画由 useCurrentFrame、interpolate 和 Sequence 驱动。
- [x] 代码高亮、数组槽位、live objects 和析构计数来自同一帧状态。
- [x] 组合已注册为 DeleteArrayQ11。
- [x] trace.json 已创建，帧区间与组件时间线一致。
- [x] npm run lint 通过。

## 渲染与成片核对

- [x] 完整 MP4 已渲染；最终版为修正配对状态后的第二次完整渲染结果。
- [x] ffprobe 确认 1920×1080、30/1 fps、H.264、AAC；视频时长 218.000 秒，6540 帧。
- [x] 全帧解码和亮度扫描通过：6540/6540 帧；最低灰度均值 240.4936；灰度均值低于 40 的帧为 0。
- [x] 开头、答案地图、单对象、数组、配对、悬空指针、嵌入式、追问和结尾关键帧已抽取到 out/Q011/review-frames-v2/。
- [x] 联系表 out/Q011/review-contact-sheet-v2.png、海报 out/Q011/Q011-poster.png 已人工查看，无文字溢出、遮挡或黑帧。
- [x] 场景边界、数组析构顺序、错误配对结果框和最终复述已复核。
- [x] 按成片时间线完成关键状态的语义复核，代码高亮与字幕结论一致。

## 修正记录

- 第一版完整渲染后，人工检查发现 PairingScene 的当前案例使用了错误元组下标：错误配对帧把“层级不匹配”显示在结果框中，颜色也未严格跟随错误状态。
- 已将当前案例统一改为从同一元组派生含义、结果和颜色，并重新执行 lint、完整渲染、ffprobe、全帧扫描和人工查看。
- 第二版最终帧已显示红色“未定义行为”，不再承诺固定析构次数或固定崩溃结果。

## 最终复核结论

1. 单对象场景明确区分 delete 的一次析构与对应存储释放。
2. 数组场景显示 3 个对象、逐个析构和 items[2] → items[1] → items[0] 的结束顺序。
3. 错误配对场景统一使用“未定义行为”，当前案例红色结果框与左侧高亮同步。
4. 悬空指针、p = nullptr 和 delete nullptr 的边界完整可读。
5. 本题实际视频时长 3:38，符合中等题 4 分钟建议上限，准予归档。
