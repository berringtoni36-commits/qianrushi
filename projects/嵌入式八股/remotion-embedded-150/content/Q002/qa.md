# Q002 QA

## 内容与事实

- [x] 核心答案准确表达 `const` 的只读语义和编译期约束。
- [x] 三种指针组合的“指向/内容”判断与代码示例一致。
- [x] 嵌入式查表、只读参数和固定配置三个场景均带有工程实现边界。
- [x] `const`、`#define`、`const_cast`、线程同步没有混成同一层次的保证。
- [x] 最终边界代码使用 `undefined.cpp` 和显式 `const_cast<int *>`，与标题一致。

## 分镜与状态

- [x] 共 7 个独立场景，帧区间连续覆盖 0-6899，无隐藏开头空段。
- [x] 指针高亮、当前编号、解释和示例由同一个 `phase` 派生。
- [x] 嵌入式代码高亮与右侧用法卡由同一个 `active` 派生。
- [x] 四个追问分别保留 5 秒回忆时间，答案绑定场景本地帧。
- [x] 四个最终复述分别保留 4 秒回忆时间，答案淡入不超出场景时长。
- [x] 逐项抽查了场景入口、状态切换前后、答案稳定帧和场景结束前帧。

## 视觉与节奏

- [x] 开头、答案地图、指针表格、嵌入式用法、边界代码、追问和最终复述均已人工抽帧检查。
- [x] 首轮抽查发现边界场景的标题/代码语言不一致，已修正后重新渲染；最终抽查未发现未解决的遮挡、溢出或不可读文字。
- [x] 修正版代表帧已检查：0、6、18、30、40、95、101、115、128、128.5、134、142、143、150、155、156、161、162、168、173、174、180、185、186、192、197、198、204、208、209、214、215、217、221、222、223、227、228、229.966 秒。
- [x] 以 1920x1080 缩小预览检查四张联系表，信息层级和重点状态保持清晰。
- [x] 最后一帧仍保留最终边界答案、总结字幕和进度条。

## 成片规格

- 目标：1920x1080，30fps，H.264 MP4，难度中等建议不超过 4:00。
- 实际：230.058667 秒（约 3:50.06），6900 视频帧，1920x1080，30fps，H.264 视频 + AAC 音频，文件大小 15,291,786 bytes。
- 全帧缩略扫描：6900/6900 帧，最低灰度均值 242.0017，`mean < 8` 的黑屏帧为 0，`mean < 32` 的暗帧为 0。
- `ffprobe` 已确认视频和音频流存在，视频帧数、分辨率和帧率符合目标规格。
- 海报：`out/Q002/Q002-poster.png` 已生成并人工检查。
- 最终复核日期：2026-08-16。

## 结论

无未解决的高优先级问题，Q002 可以标记为完成。
