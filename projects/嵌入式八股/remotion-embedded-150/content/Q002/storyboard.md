# Q002 分镜

## 场景帧区间

| 场景 | 帧区间 | 时间 | 主要任务 | 进入状态 | 最终状态 |
|---|---:|---:|---|---|---|
| Opening | 0-539 | 0:00-0:18 | 主动回忆与路线预览 | 问题、6 秒倒计时，路线卡降级显示 | 三个复习入口完整可见 |
| Map | 540-1199 | 0:18-0:40 | 建立三层答案地图 | 第一张卡先出现 | 三张卡同时可读 |
| Pointer | 1200-2849 | 0:40-1:35 | 解释三种指针组合 | 6 秒回忆，右侧表格等待揭示 | 三种声明、指向、内容和示例同步 |
| Embedded | 2850-3839 | 1:35-2:08 | 连接嵌入式工程用法 | 6 秒回忆，代码和用法卡等待揭示 | 查表、参数、配置三种用法完成高亮 |
| Boundary | 3840-4679 | 2:08-2:36 | 区分 `#define`、`const_cast` 和同步边界 | 左卡立即可读，右卡延迟淡入 | `undefined.cpp` 与未定义行为警示完整 |
| Follow-ups | 4680-6119 | 2:36-3:24 | 练习四个面试追问 | 每题先倒计时 5 秒 | 四个答案均已出现并可复述 |
| Final | 6120-6899 | 3:24-3:50 | 最终无提示复述 | 每题先停 4 秒 | 四个收束答案完成，最后一帧仍有完整结论 |

## 关键帧验收点

- `0`：问题、6 秒倒计时和待揭示路线卡可读。
- `18 秒`：答案地图第一张卡可见，后两张卡按顺序进入，不出现无意义空场。
- `40 秒`：指针场景的回忆提示出现；`46 秒` 后三种声明按阶段高亮。
- `95 秒`：嵌入式场景入口，6 秒回忆提示覆盖右侧用法卡，左侧代码仍可读。
- `128 秒`：边界场景入口，左侧 `const vs #define` 卡立即存在，右侧 `undefined.cpp` 延迟进入。
- `135 秒`：左侧边界内容激活；`143 秒`：右侧 `const_cast` 代码进入当前重点。
- `156 秒`：第一个追问开始；`162 秒`、`174 秒`、`186 秒`、`198 秒` 分别能读到四个答案的稳定状态。
- `204 秒`：最终复述第一题；`209 秒`、`215 秒`、`222 秒`、`228 秒` 分别能读到稳定答案。
- `229.966 秒`：最后一个边界答案、总结字幕和进度条仍完整可读。

## 状态一致性

指针阶段的高亮行、当前编号、声明解释和示例由同一个 `phase` 派生。嵌入式阶段的代码行和右侧用法卡由同一个 `active` 派生。追问和最终复述的答案显示都绑定各自场景的本地帧，不使用跨场景固定时间。
