# Q001 QA

## 内容与脚本

- [x] 三个 `static` 语义入口与原题一致。
- [x] 局部变量代码轨迹为初始化一次、连续输出 `1, 2, 3`。
- [x] 文件作用域示例明确表达内部链接，而不是普通块作用域。
- [x] C++ 静态成员的类归属与共享数量一致。
- [x] `.data` / `.bss` 说明带有常见实现边界，未宣称所有平台布局完全相同。
- [x] 线程安全追问明确回答“`static` 本身不保证线程安全”。

## 分镜与状态

- [x] 每个场景有独立帧区间，状态由 `useCurrentFrame()` 和明确的帧阈值驱动。
- [x] 追问答案揭晓帧跟随实际 prompt 时长，不使用旧版本固定帧数。
- [x] 结尾答案淡入跟随每个复述块的真实帧长。
- [x] 取消旧版根容器 `from={590}`，避免正式成片在开头产生隐藏空白段。

## 视觉

- [x] 初次渲染静帧检查：发现并记录场景起始淡入空白问题
- [x] 开头、中段、难点、结尾静帧检查：主体画面、文字和状态可读
- [x] 缩小预览可读性检查：1920x1080 画面层级清楚
- [x] 成片边界与黑帧检查：已复核所有场景边界；全片 8430 帧缩略灰度扫描无黑屏阈值帧，最低平均亮度为 240.76。

## 成片规格

- 目标：1920x1080，30fps，H.264 MP4，目标时长不超过 4:45。
- 实际：281.045 秒（4:41.05），8430 帧，1920x1080，30fps，H.264 视频 + AAC 音频，文件大小 18,979,512 bytes。
- 关键画面复核：开头、7 秒路线图、局部 `cnt = 1/2/3`、文件内部链接错误、C++ 两对象共享计数、`.data` / `.bss`、四个追问答案、四段最终复述、最后一帧。
- 海报 `out/Q001/Q001-poster.png` 已生成并人工检查。
- 最终复核日期：2026-08-16。
