# Q007 分镜

## 场景帧区间

| 场景 | 帧区间 | 时间 | 主要任务 | 进入状态 | 最终状态 |
|---|---:|---:|---|---|---|
| Opening | 0-479 | 0:00-0:16 | 主动回忆与路线预览 | 问题、6 秒倒计时、路线卡降级显示 | 三个判断入口完整可见 |
| Map | 480-1019 | 0:16-0:34 | 建立字节数/字符串/缓冲区地图 | 三张卡按顺序进入 | 三个概念同时可读 |
| String | 1020-2339 | 0:34-1:18 | 追踪 `str`、`\\0`、`sizeof`、`strlen` 和指针 | 7 秒回忆，五个状态等待揭示 | 4、3、指针大小三个结果对齐 |
| Buffer | 2340-3539 | 1:18-1:58 | 展示二进制 `0x00` 提前截断 | 6 秒回忆，四个扫描阶段 | 真实长度 3 与 `strlen` 长度 1 分开 |
| Traps | 3540-4559 | 1:58-2:32 | 对比数组参数、VLA 和空类型边界 | 5 秒回忆，四个陷阱逐个激活 | 主规则和例外并列可读 |
| Follow-ups | 4560-5999 | 2:32-3:20 | 练习四个面试追问 | 每题先倒计时 5 秒 | 四个答案稳定出现 |
| Final | 6000-6779 | 3:20-3:46 | 最终无提示复述 | 每题先停 4.2 秒 | 四个收束答案完成，最后一帧仍可读 |

## 关键帧验收点

- `0`：问题、6 秒倒计时和待揭示路线卡可读。
- `480`：答案地图开始，三张卡分别在本地帧 `0`、`26`、`52` 后进入。
- `1020`：字符串场景入口；本地帧 `0-209` 为 7 秒回忆，随后每 210 帧切换一次状态。
- `1320`、`1530`、`1740`、`1950`、`2160`：五个字符串状态稳定区间。
- `2340`：二进制场景入口；本地帧 `0-179` 为 6 秒回忆，随后每 240 帧切换一次扫描阶段。
- `2580`、`2820`、`3060`、`3300`：四个缓冲区状态稳定区间。
- `3540`：易错点场景入口；本地帧 `0-149` 为 5 秒回忆，随后每 210 帧激活一个陷阱。
- `4560`、`4710`、`4920`、`5160`、`5520`、`5880`：追问入口与答案揭晓附近。
- `6000`、`6126`、`6321`、`6516`、`6711`：最终复述入口与四个答案稳定附近。
- `6779`：最后一个答案、总结字幕和进度条仍完整可读。

## 状态一致性

字符串场景的代码高亮、当前阶段和五张关系卡由同一个 `phase` 派生；二进制场景的代码高亮、字节扫描高亮和结论卡由同一个 `phase` 派生；易错点场景的当前卡片由同一个 `phase` 派生。
