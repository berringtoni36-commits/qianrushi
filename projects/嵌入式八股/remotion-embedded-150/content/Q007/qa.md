# Q007 QA

## 内容与事实

- [x] 核心答案准确区分 `sizeof` 的字节数和 `strlen` 的 C 字符串长度。
- [x] `char str[] = "abc"` 的 `sizeof(str) = 4`、`strlen(str) = 3` 已在代码、字幕和复述中保持一致。
- [x] `char *p = str` 后，`sizeof(p)` 表达指针变量大小，`strlen(p)` 仍为 3，没有把平台相关的指针宽度写成固定常数。
- [x] 二进制 `0x00` 提前截断、函数参数数组退化、VLA 运行时 `sizeof` 和空类型标准边界已明确。

## 分镜与状态

- [x] 共 7 个独立场景，帧区间连续覆盖 0-6779。
- [x] 字符串场景的代码高亮、当前阶段和五张关系卡由同一个 `phase` 派生。
- [x] 二进制场景的代码高亮、字节高亮和结论卡由同一个 `phase` 派生。
- [x] 四个追问保留 5 秒回忆时间，四个最终复述保留 4.2 秒回忆时间。

## 视觉与节奏

- [x] 开头、答案地图、字符串状态、二进制扫描、易错边界、追问和最终复述均完成人工抽帧检查。
- [x] 代表帧覆盖场景入口、每个状态切换后一帧、答案首次出现帧和最后一帧；检查集保存在 `out/Q007/review-frames-v2/`。
- [x] 通过缩小联系表和 1920x1080 全尺寸关键帧检查文字层级、状态颜色、代码高亮和底部字幕；已修复字符串关系卡被 Caption 遮挡的问题。
- [x] 最终海报 `out/Q007/Q007-poster.png` 已重新生成并人工检查。

## 成片规格

- 目标：1920x1080，30fps，H.264 MP4，中等题不超过 4:00。
- 实际：226.000000 秒，6780 个视频帧，1920x1080，30fps，H.264 视频 + AAC 音频，文件大小 15,451,091 bytes；音频时长 226.048000 秒。
- 全帧扫描：6780/6780 帧完成；最低灰度均值 241.0218（帧 4536），`mean < 8` 的黑屏帧为 0，`mean < 32` 的暗帧为 0。

## 结论

结论：无未解决的高优先级问题。2026-08-16 完成修复后重渲染、全帧扫描、关键帧与缩小联系表复核，Q007 可以标记为完成。
