# Q004 QA

## 内容与事实

- [x] 核心答案准确表达 `extern` 的声明、定义和跨文件符号语义。
- [x] `sensor.c` 定义、`sensor.h` 声明、`main.c` 使用和链接器解析顺序一致。
- [x] 重复定义、未定义符号和头文件直接定义三类错误均有对应后果。
- [x] `extern "C"` 的 C++ 链接约定与全局变量工程耦合边界均有说明。
- [x] 没有把 `extern` 说成线程安全、封装或存储分配机制。

## 分镜与状态

- [x] 共 7 个独立场景，帧区间连续覆盖 0-7019，无隐藏开头空段。
- [x] 链路代码高亮、当前阶段、文件名和右侧状态卡由同一个 `phase` 派生。
- [x] 四个追问分别保留 5 秒回忆时间，四个最终复述分别保留 5.5 秒回忆时间。
- [x] 抽查了场景入口、链路阶段切换、错误日志重点、答案稳定帧和最后一帧。

## 视觉与节奏

- [x] 开头、答案地图、跨文件链路、错误对比、C++ 边界、追问和最终复述均已人工抽帧检查。
- [x] 代表帧覆盖 0、6、18、30、39、40、47、52、64、76、88、94、95、100、108、115、126、127、134、145、151、152、157、164、169、176、181、188、193、200、205、208.5、213.5、217、222、225.5、231、233.966 秒。
- [x] 通过 1920x1080 缩小联系表检查文字层级、状态颜色和底部字幕，没有发现遮挡、溢出或不可读文本。
- [x] 最终海报 `out/Q004/Q004-poster.png` 已生成并人工检查。

## 成片规格

- 目标：1920x1080，30fps，H.264 MP4，中等题建议不超过 4:00。
- 实际：234.048000 秒（约 3:54.05），7020 视频帧，1920x1080，30fps，H.264 视频 + AAC 音频，文件大小 15,739,619 bytes。
- 全帧缩略扫描：7020/7020 帧，最低灰度均值 241.2899，`mean < 8` 的黑屏帧为 0，`mean < 32` 的暗帧为 0。
- `ffprobe` 已确认视频和音频流存在，视频帧数、分辨率和帧率符合目标规格。
- 最终复核日期：2026-08-16。

## 结论

无未解决的高优先级问题，Q004 可以标记为完成。
