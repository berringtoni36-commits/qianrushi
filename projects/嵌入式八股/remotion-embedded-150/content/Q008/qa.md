# Q008 QA

## 内容与事实

- [x] 核心答案区分栈的调用帧/自动存储模型与堆的动态分配/显式释放模型。
- [x] 没有把 C/C++ 标准强制成“所有平台都有固定栈和堆布局”。
- [x] 栈的 LIFO、空间限制、溢出风险和堆的碎片、泄漏、失败、时延边界表达准确。
- [x] Linux 用户态虚拟地址、裸机 MCU 内存映射、FreeRTOS 任务栈和水位监控边界明确。

## 分镜与状态

- [x] 共 7 个独立场景，帧区间连续覆盖 0-7019。
- [x] 调用栈场景的代码高亮、栈帧列表和当前阶段由同一个 `phase` 派生。
- [x] 堆生命周期场景的代码高亮、H1 状态和结论卡由同一个 `phase` 派生。
- [x] 四个追问保留 5 秒回忆时间，四个最终复述保留 4.2 秒回忆时间。

## 视觉与节奏

- [x] 开头、答案地图、调用栈、堆生命周期、嵌入式边界、追问和最终复述均完成人工抽帧检查。
- [x] 代表帧覆盖场景入口、每个状态切换后一帧、答案首次出现帧、答案完全展开帧和最后一帧。
- [x] 通过缩小联系表和 1920x1080 全尺寸关键帧检查文字层级、状态颜色、代码高亮和底部字幕，没有遮挡、溢出或不可读文本。
- [x] 最终海报已生成并人工检查。

复核产物：`out/Q008/review-frames-v2/`，包含 49 个状态/入口关键帧、答案完全展开帧和五组联系表。

## 成片规格

- 目标：1920x1080，30fps，H.264 MP4，中等题不超过 4:00。
- 实际：视频流 234.000000 秒、7020 帧、1920x1080、30fps、H.264；音频 AAC，48kHz，双声道，234.048000 秒；文件大小 16,136,708 bytes。
- 全帧扫描：7020/7020 帧完成；64x36 灰度均值最低 240.1701（帧 4696，156.533 秒），`mean < 8` 的黑屏帧为 0，`mean < 32` 的暗帧为 0。

## 结论

结论：无未解决的高优先级问题。2026-08-16 完成渲染、全帧扫描、关键帧与联系表复核，Q008 可以标记为完成。
