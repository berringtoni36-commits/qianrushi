# Q005 QA

## 内容与事实

- [x] 核心答案准确区分数组空间与指针地址。
- [x] 内存轨迹中指针可以移动/改指向，而数组本体不变且不能整体赋值。
- [x] `sizeof(arr)` 与 `sizeof(buf)` 的上下文差异表达正确。
- [x] 指针数组、数组指针和“指针 + 长度”接口均与题库原文一致。
- [x] 指针大小以 `sizeof(int *)` 表达，没有把平台相关数值说成统一常数。

## 分镜与状态

- [x] 共 7 个独立场景，帧区间连续覆盖 0-7469，无隐藏开头空段。
- [x] 内存场景的代码高亮、当前阶段和关系卡由同一个 `phase` 派生。
- [x] 退化场景的代码行、当前阶段和参数卡由同一个 `active` 派生。
- [x] 四个追问分别保留 5 秒回忆时间，四个最终复述分别保留 5.5 秒回忆时间。
- [x] 抽查了场景入口、内存状态切换、参数退化状态、答案稳定帧和最后一帧。

## 视觉与节奏

- [x] 开头、答案地图、内存关系、数组退化、对比陷阱、追问和最终复述均已人工抽帧检查。
- [x] 代表帧覆盖 0、6、18、30、39、40、47、57、67、77、90、94、95、101、104、113、122、134、135、141、149、166、167、173、179、185、191、197、203、209、215、221、223.5、229、232、238、240.5、246、248.966 秒。
- [x] 通过 1920x1080 缩小联系表检查文字层级、状态颜色和底部字幕，没有发现遮挡、溢出或不可读文本。
- [x] 最终海报 `out/Q005/Q005-poster.png` 已生成并人工检查。

## 成片规格

- 目标：1920x1080，30fps，H.264 MP4，困难题不超过 5:00。
- 实际：249.045333 秒（约 4:09.05），7470 视频帧，1920x1080，30fps，H.264 视频 + AAC 音频，文件大小 16,249,456 bytes。
- 全帧缩略扫描：7470/7470 帧，最低灰度均值 241.9323，`mean < 8` 的黑屏帧为 0，`mean < 32` 的暗帧为 0。
- `ffprobe` 已确认视频和音频流存在，视频帧数、分辨率和帧率符合目标规格。
- 最终复核日期：2026-08-16。

## 结论

无未解决的高优先级问题，Q005 可以标记为完成。
