# Q005 分镜

## 场景帧区间

| 场景 | 帧区间 | 时间 | 主要任务 | 进入状态 | 最终状态 |
|---|---:|---:|---|---|---|
| Opening | 0-539 | 0:00-0:18 | 主动回忆与路线预览 | 问题、6 秒倒计时，路线卡降级显示 | 三个判断入口完整可见 |
| Map | 540-1199 | 0:18-0:40 | 建立本质地图 | 数组卡先出现 | 数组、指针、参数三层同时可读 |
| Memory | 1200-2849 | 0:40-1:35 | 追踪数组空间和指针地址 | 7 秒回忆，关系卡等待揭示 | 数组创建、指向、移动、改指向四阶段完成 |
| Decay | 2850-4049 | 1:35-2:15 | 解释函数参数退化 | 6 秒回忆，参数卡等待揭示 | 原数组大小、参数指针和安全接口对齐 |
| Traps | 4050-5009 | 2:15-2:47 | 区分 `sizeof`、赋值和括号声明 | 左卡先可读，右卡延迟进入 | 指针数组与数组指针完整对比 |
| Follow-ups | 5010-6449 | 2:47-3:35 | 练习四个面试追问 | 每题先倒计时 5 秒 | 四个答案稳定出现 |
| Final | 6450-7469 | 3:35-4:09 | 最终无提示复述 | 每题先停 5.5 秒 | 四个收束答案完成，最后一帧仍可读 |

## 关键帧验收点

- `0`：问题、6 秒倒计时和待揭示路线卡可读。
- `18 秒`：答案地图第一张数组卡可见，后两张卡按顺序进入。
- `40 秒`：内存关系回忆提示出现；`47 秒` 后开始展示数组与指针阶段。
- `57 秒`、`67 秒`、`77 秒`：指针指向、移动和改指向状态顺序正确。
- `95 秒`：函数参数退化入口和 6 秒回忆提示出现；`104 秒`、`113 秒`、`122 秒` 分别高亮三个参数阶段。
- `135 秒`：对比场景入口；`141 秒` 左侧能力卡激活，`149 秒` 右侧括号声明出现。
- `167 秒`：第一个追问开始；`173 秒`、`185 秒`、`197 秒`、`209 秒` 是四个答案稳定状态附近。
- `215 秒`：最终复述开始；`221 秒`、`229 秒`、`238 秒`、`246 秒` 是四个答案稳定状态附近。
- `248.966 秒`：最后一个数组指针答案、总结字幕和进度条仍完整可读。

## 状态一致性

内存场景的代码高亮、当前阶段和关系卡由同一个 `phase` 派生。退化场景的代码行与参数阶段由同一个 `active` 派生，答案显示绑定各场景本地帧。
