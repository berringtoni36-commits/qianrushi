# Q003 分镜

## 场景帧区间

| 场景 | 帧区间 | 时间 | 主要任务 | 进入状态 | 最终状态 |
|---|---:|---:|---|---|---|
| Opening | 0-539 | 0:00-0:18 | 主动回忆与路线预览 | 问题、7 秒倒计时，路线卡降级显示 | 三个回答入口完整可见 |
| Map | 540-1259 | 0:18-0:42 | 建立外部改值者地图 | 中断卡先出现 | 三类外部改值者同时可读 |
| Loop | 1260-2999 | 0:42-1:40 | 追踪轮询与 ISR 写入 | 7 秒回忆，代码和轨迹卡等待揭示 | 四个状态按顺序完成：读 0、优化风险、ISR 写 1、下一次读 1 |
| Scenarios | 3000-4139 | 1:40-2:18 | 连接三类工程场景 | 6 秒回忆，三张场景卡等待揭示 | 中断、寄存器、DMA/RTOS 三种用法完成高亮 |
| Limits | 4140-5039 | 2:18-2:48 | 区分可见性、原子性和同步 | 左卡立即可读，右卡延迟进入 | `volatile` 能做/不能做与 `const volatile` 完整可读 |
| Follow-ups | 5040-6479 | 2:48-3:36 | 练习四个面试追问 | 每题先倒计时 5 秒 | 四个答案均稳定出现 |
| Final | 6480-7799 | 3:36-4:20 | 最终无提示复述 | 每题先停 6 秒 | 四个收束答案完成，最后一帧仍可读 |

## 关键帧验收点

- `0`：问题、7 秒倒计时和待揭示路线卡可读。
- `18 秒`：答案地图第一张卡可见，后两张卡按顺序进入。
- `42 秒`：轮询机制入口出现；`49 秒` 回忆层结束并开始显示状态轨迹。
- `61 秒`、`73 秒`、`85 秒`：四阶段中的状态高亮分别转移到优化风险、ISR 写入和下一次观察。
- `100 秒`：工程场景入口，6 秒回忆提示出现；`106 秒`、`115 秒`、`124 秒` 分别高亮三类场景。
- `138 秒`：边界场景入口；`144 秒` 左侧能力卡激活，`152 秒` 右侧限制和 `const volatile` 代码稳定可读。
- `168 秒`、`180 秒`、`192 秒`、`204 秒`：四个追问开始；`174 秒`、`186 秒`、`198 秒`、`210 秒` 的答案稳定可读。
- `216 秒`：最终复述开始；`223 秒`、`234 秒`、`245 秒`、`256 秒` 的四个答案稳定可读。
- `259.966 秒`：最后一个 `const volatile` 答案、总结字幕和进度条仍完整可读。

## 状态一致性

轮询代码高亮、当前阶段、读值文本和 ISR 写入说明由同一个 `phase` 派生。工程场景代码行与右侧卡片由同一个 `active` 派生。追问和最终复述的答案显示都绑定场景本地帧，状态不会跨场景漂移。
