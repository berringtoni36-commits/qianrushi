# Q010 QA 记录

## 制作状态

- 状态：complete
- 最后更新：2026-08-17
- 目标视频：`out/Q010/Q010-malloc-new.mp4`
- 目标规格：1920×1080 / 30fps / H.264 MP4
- 实际时长：238.058667 秒（3:58）
- 视频帧：7140 / 7140
- 文件大小：16,453,633 bytes

## 内容核对

- [x] 原题、核心结论和追问已从第 10 题原文提取。
- [x] `malloc/free` 与 `new/delete` 的抽象层次已分开。
- [x] 对象生命周期用 placement new 场景说明，避免把“类型指针”误画成“已构造对象”。
- [x] `new[]/delete[]` 的数组析构次数已由同一阶段状态驱动。
- [x] 错误配对统一标记为未定义行为，没有承诺固定的错误结果。
- [x] 普通 `new`、`new (std::nothrow)` 和 `malloc` 的失败形式已区分。
- [x] 嵌入式建议包含静态分配、固定块池、失败处理和所有权边界。

## 代码与结构核对

- [x] 所有动画使用 `useCurrentFrame()` / `interpolate()` / `Sequence`。
- [x] 代码高亮和状态卡由显式 `phase` 派生。
- [x] 组合已注册为 `MallocNewQ10`。
- [x] 追踪文件已创建：`content/Q010/trace.json`。
- [x] `npm run lint` 通过。

## 渲染与成片核对

- [x] 完整 MP4 已渲染，最终版为第二次完整渲染结果。
- [x] `ffprobe` 已确认 1920×1080、30/1 fps、H.264、AAC、238.058667 秒。
- [x] 全帧亮度扫描无黑屏、暗帧或异常空白：7140/7140 帧；最低灰度均值 240.4109；灰度均值低于 40 的帧为 0。
- [x] 开头、答案地图、原始内存、数组、错误配对、嵌入式、追问、结尾关键帧已抽取到 `out/Q010/review-frames-v2/`。
- [x] 联系表、代表关键帧和 `out/Q010/Q010-poster.png` 已人工查看，无文字溢出、遮挡或高亮错位。
- [x] 场景边界和状态切换复核通过；数组末段明确显示 `live objects: 0`、`3 ctor`、`3× dtor`。
- [x] 已对成片完成全帧解码检查，并对关键场景进行 1 倍速语义复核。

## 最终复核结论

1. Raw 场景中 `static_cast<Device*>(storage)` 与 placement new 的构造阶段分离，状态同步正确。
2. Arrays 场景中 `new Device[3]` 和 `delete[] many` 的高亮、构造计数、live object 数量和析构次数同步正确。
3. Mismatch 场景明确显示“未定义行为”，没有把错误结果简化成固定析构次数。
4. Followups 和 Final 的长中文答案均在画面内完整可读。
5. 本题满足中等题 4 分钟建议上限，最终 3:58，准予归档。
