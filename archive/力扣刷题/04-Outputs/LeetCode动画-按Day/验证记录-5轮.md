# Hot100 动画 · 五轮审查记录

审查对象：`LeetCode动画-按Day/` 下的 14 个 Day 文件夹、100 道题的 HTML 与 Obsidian 预览。

质量基线：第 21 题「合并两个有序链表」的逐状态链表样板。第 21 题原样保留到 Day 4；其余页面改为按题型建模，不再使用旧版“6 张通用卡片 + 抽象状态”作为最终交付。

## 第 1 轮：结构与源码一致性

- ✓ 100 个 `index.html`、100 个 `Obsidian预览.md`，14 个 Day 入口全部存在。
- ✓ Day 分组从原始笔记 `04-Hot100两周速通-yxc简洁代码.md` 逐题读取：Day 1–14 共 100 题。
- ✓ 99 个新引擎页面保留原始 YXC C++ 代码逐字符一致；第 21 题使用已验收样板。
- ✓ 每个新引擎页面 8 个教学状态；代码高亮行号全部落在实际源码范围内。
- ✓ 100/100 页面都含“力扣官方题目”折叠抽屉和官方题目链接。
- ✓ 题面区域单独 `max-height + overflow:auto`，不与动画工作区共用滚动条。

## 第 2 轮：Chrome 交互与视觉回归

- ✓ 15 个代表页面覆盖哈希、窗口、数组、链表、树、图/网格、栈、单调栈、回溯、二分、DP、技巧等题型。
- ✓ 代表页面逐步点击“下一步”：步骤计数递增、状态画面变化、C++ 当前行高亮同步。
- ✓ 第 20 题实测为专属栈模型：输入游标、栈顶、入栈/出栈、匹配失败边界都可见。
- ✓ 第 21 题 Day 4 复制页保持 22 帧样板、官方题面和原有控制器。
- ✓ 100/100 页面实际打开回归：无横向溢出、无应用级滚动穿透、下一步控制存在。
- ✓ 代码高亮经过修复：C++ 关键字、类型、数字、注释正常显示，没有泄漏 `<span>` 或 HTML 实体。
- ✓ 播放控制补齐 0.75× / 1× / 1.5× / 2× 四档速度；每次切帧有轻微状态过渡，并尊重系统减少动效设置。
- ✓ 步骤滑块同步写入 `aria-valuetext`（如“读题：从空栈开始”），键盘/辅助操作能知道当前帧含义。
- ✓ 最终页浏览器错误日志为空。

## 第 3 轮：Obsidian 浅色/深色适配

- ✓ 在 Obsidian 阅读视图真实加载 Day 1「两数之和」预览：iframe 成功读取同目录 HTML，官方题面折叠、动画和控制按钮可见。
- ✓ 同一预览在 Obsidian 内点击“下一步”后从 `1 / 8` 变为 `2 / 8`，说明不是静态截图。
- ✓ 在 Obsidian 内点击主题按钮验证浅色 → 暗色：深色背景、面板、代码区、题面抽屉和强调色均正确切换。
- ✓ 在 Obsidian 真实加载 Day 8 第 20 题预览：栈输入、栈顶状态、题面和 C++ 代码均正常显示。
- △ Obsidian 的 iframe 隔离会让“自动跟随宿主外观”在不同版本/窗口状态下不一定能读取宿主 CSS；因此每页保留明确的“自动 / 浅色 / 暗色”按钮，暗色已在真实 Obsidian 预览中验证可用。

## 第 4 轮：学习内容与题型覆盖审查

- ✓ 28 种题型模式均有专属视觉状态：哈希表、窗口、链表、环、栈、树、图、Trie、网格、DP、二分、堆、LRU、回溯、区间、双指针等。
- ✓ 每页 8 帧都包含“输入 → 建立不变量 → 观察 → 核心动作 → 更新 → 边界 → 收束 → 返回”的学习弧线。
- ✓ 题型视觉与模式匹配：网格使用网格单元，窗口使用 l/r，链表使用节点与指针，栈使用栈顶，DP 使用状态表，二分使用 lo/mid/hi。
- ✓ 99 个新引擎页面的核心帧不再高亮源码第 1 行占位；每个核心帧都落到可定位的实际代码行。
- ✓ 每个页面的示例、期望结果、易错点和官方题面来自原始题解/Wiki，不修改原始笔记。
- ✓ 语义抽查后收紧了高风险题：第 1 题按 `complement → 查询 → 写入 → 命中` 演化，第 3 题真实推进 `l/r/window`，第 5 题改为中心扩展（字符中心与间隙中心），第 20 题按 `([{}])` 真实入栈/出栈，第 42 题按源码改为单调栈。
- ✓ 另外为第 48/54/73 题加入转置、螺旋边界、行列标记的矩阵状态，为第 287 题加入数组隐式环的 slow/fast 状态；避免“题目模式正确但画面语义不对”。
- ✓ 每个页面新增折叠的“题解原意与易错点”，步骤首帧/收束帧会带出原始笔记中的思路与易错提醒，动画状态和 C++ 源码对照关系更明确。

## 第 5 轮：修复后最终回归

- ✓ 修复代码高亮实体泄漏后重新生成全部 100 页，并重新通过 HTML 内联脚本 VM 解析。
- ✓ 增加网格/网格 BFS 专属可视化后重新生成全部 100 页，并重新检查所有步骤行号。
- ✓ 对照第 21 题样板补齐速度选择器与状态切换过渡，再次确认栈题控制器可见、可操作。
- ✓ 再次执行 100 页 Chrome 全量加载与布局回归：`checked=100, failures=0`。
- ✓ 再次执行 100 页静态结构审计：HTML 100、Obsidian 预览 100、失败 0。
- ✓ 交付目录只新增 `LeetCode动画-按Day/`；原始题解和旧输出目录未被删除或覆盖。
- ✓ 最新一轮静态审计：100/100 页 JSON 数据、8 帧、源码行号、官方题面、思路/易错字段齐全；核心帧没有源码第 1 行占位；100/100 内联脚本通过 `node --check`。
- ✓ 最新 Chrome 全量导航：100/100 页可加载；新引擎页均有动画区、下一步、速度选择器且无横向溢出；第 21 题样板单独按其 `链表状态 / POINTER STATE` 结构复核。
- ✓ 第 1、5、20 题再次在 Chrome 实际逐帧点击；第 1 题验证速度选择与暂停，第 20 题验证走完 8/8；官方题面和题解抽屉展开后控制区仍可见。
- ✓ Obsidian 最新实机复核 Day 1：成功加载 iframe，浅色→暗色按钮切换后背景/面板/代码区同步变暗，暗色下“下一步”从 1/8 变为 2/8；页面仍无横向溢出。
- ✓ 追加语义抽查：第 53 题展示 `current/best` 的 Kadane 状态，第 121 题展示 `min_price/max_profit`，94/98/102/104/230 展示中序、层序、BST 不变量或递归高度；这些页面不再只显示无关的通用树/DP 占位。
- ✓ 独立复核提出的行号问题已修正：第 1 题现在逐帧落到 `r = target - nums[i]`、`heap.count`、写入和命中返回；第 5 题落到奇/偶中心两段 `while` 与 `substr`；第 20 题落到 `stk.push`、`stk.pop` 和 `return stk.empty()`。
- ✓ 进一步补强了 438/76 窗口频次、155/394/739/84 栈、46/78/22/51 回溯、295 双堆页面；这些状态使用题目示例中的字符、数字、路径或堆内容，而不是空泛占位。

## 当前交付判断

五轮中没有留下会阻塞阅读、单步操作、题面查看或代码对照的严重问题。新入口从 [README.md](README.md) 开始；每个 Day 入口再进入对应题目 HTML 或 Obsidian 预览。第 21 题继续作为链表逐状态质量基线，第 5 题则使用中心扩展专属样板，不再套用二维 DP 表。
