# LeetCode 21 动画验证记录

验证日期：2026-08-16

## 验证范围

- 独立 HTML：`index.html`
- Obsidian 阅读视图承载：`Obsidian预览.md`
- 主案例：`l1 = [1,2,4]`，`l2 = [1,3,4]`
- 边界案例：`l1 = []`，`l2 = [0]`
- 代码行以同目录动画中的 YXC C++17 原文为准；未改动原始题解笔记。

## 算法与交互

- HTML 内置不可变步骤快照，节点身份、`next` 链接、`l1/l2/tail/dummy` 指针和 `dummy.next` 同步渲染。
- 主案例完整 22 帧通过：代码行序列为
  `15,14,16,20,21,16,17,18,16,17,18,16,20,21,16,20,21,15,24,24,25,26`。
- 主案例最终结果为 `1 → 1 → 2 → 3 → 4 → 4`，最终高亮 `return dummy->next`，下一步按钮禁用。
- 边界案例完整 7 帧通过：代码行序列为
  `15,14,15,24,25,25,26`；最终结果为 `0`，下一步按钮禁用。
- 已验证上一步、下一步、重置、步骤滑块、播放/暂停和播放速度控件。
- 已验证每帧结果有序、来源节点顺序单调，且最终节点身份与期望一致。

## 主题、字体与布局

- 主题按钮支持 `自动 → 浅色 → 暗色 → 自动`，并持久化手动选择；Obsidian 预览通过 `?theme=auto` 强制跟随宿主/系统。
- 浅色采用 Obsidian Baseline 的暖纸张、紫色强调色和半透明表面；暗色采用深色背景、青绿色状态和橙色比较提示。
- 面板使用半透明背景与 `backdrop-filter`，允许 Obsidian 壁纸/半透明效果透出；独立 HTML 无法直接读取宿主壁纸像素，因此采用宿主可见的透明层与系统主题媒体查询适配。
- 正文优先使用 `iA Writer Quattro V / Inter / PingFang SC`，代码优先使用 `iA Writer Mono V / Fira Code`；Chrome 中字体加载检查通过。
- Chrome 与 Obsidian 中均确认页面外层无额外纵向滚动；代码 1–28 行在当前窗口高度内可见。

## Obsidian 实测

- `file://` 绝对路径 iframe 在 Obsidian 阅读视图中加载正常；同目录相对路径在 Obsidian 中为空白，因此正式预览使用绝对路径并保留 `loading="eager"`。
- 曾将 Obsidian 外观切换为深色并完成主案例 22 帧与边界案例 7 帧回归，嵌入动画随之切换为暗色；随后已恢复 Obsidian 外观为“跟随系统”。
- 当前正式预览打开后无快捷切换浮层，阅读视图可直接操作动画。

## 使用入口

1. 在 Obsidian 阅读视图打开本目录的 `Obsidian预览.md`。
2. 需要独立查看时直接打开 `index.html`；也可由本地 HTTP 静态服务器提供。
3. 先用主案例的“下一步”观察“比较 → 接入 → 移动指针”，再切到边界案例理解 while 直接跳过时为什么仍能安全接上剩余链表。

