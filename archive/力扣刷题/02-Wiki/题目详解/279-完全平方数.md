---
title: "279. 完全平方数 (Medium)"
tags: [tech, reference]
created: 2026-07-15
type: permanent
summary: "C++17 Hot 100 项目中的《279. 完全平方数 (Medium)》。"
---

# 279. 完全平方数 (Medium)

> **专题归类：** [[10-动态规划|10-动态规划]]
> **LeetCode 题目：** [打开题目](https://leetcode.cn/problems/perfect-squares/)
> **AcWing 题目：** [打开题目](https://www.acwing.com/activity/content/problem/content/2647/)
> **AcWing yxc 代码：** [打开代码](https://www.acwing.com/activity/content/code/content/451625/)
> **YXC 最终提交版：** [[Hot100两周速通-yxc简洁代码#8. 完全平方数（LeetCode 279 · Medium）|直达本题最终代码]]

---

## 题目描述

给你一个整数 n，返回和为 n 的完全平方数的最少数量。

完全平方数是一个整数，其值等于另一个整数的平方；换句话说，其值等于一个整数自乘的积。例如，1、4、9 和 16 都是完全平方数，而 3 和 11 不是。

**示例 1：**
```
输入：n = 12
输出：3
解释：12 = 4 + 4 + 4
```

**示例 2：**
```
输入：n = 13
输出：2
解释：13 = 4 + 9
```

**提示：**
- 1 <= n <= 10^4

---

## 🧩 题目详细分析

- **数据范围**：n <= 10^4，完全平方数有 sqrt(10^4) = 100 个（1², 2², ..., 100²）。
- **核心约束**：每个完全平方数可以无限次使用（因为 4=1+1+1+1 也合法），这是一个**完全背包**问题。
- **边界条件**：n=1 时最少 1 个（1=1²）；n 本身就是完全平方数时最少为 1。
- **隐藏条件**：从数学上，根据四平方和定理，任何正整数都可以表示为不超过 4 个完全平方数之和。但本题仍需计算精确最小值。

---

## 👶 小白版直白理解

你要用"完全平方数"（1,4,9,16,25,...）来拼成一个目标数字 n，就像用不同面值的硬币凑钱。每个"硬币"可以重复使用，你想让使用的硬币数量最少。比如要凑 12，用三个 4 比用 12 个 1 好。这就是"最少硬币"问题，只不过硬币面额是平方数。

---

## 💡 解题思路

### 思路一：完全背包 DP（教学优先）

**核心洞察**：将 1², 2², ..., 100² 视为物品，每个可以无限使用，目标是凑成和 n。对于每个金额 i，尝试减去一个平方数 j²，看剩下 i-j² 的最优解 + 1。

**DP 五步法：**
1. **dp 定义**：`dp[i]` 表示和为 i 的完全平方数的最少个数
2. **递推公式**：`dp[i] = min(dp[i], dp[i - j*j] + 1)`，j 从 1 到 sqrt(i)
3. **初始化**：`dp[0] = 0`，`dp[1..n] = inf`（或一个很大的数）
4. **遍历顺序**：从 i=1 到 n 正序（完全背包允许重复选取）
5. **举例验证**：n=12 → dp[0]=0, dp[1]=1, dp[2]=2, ..., dp[4]=1, dp[8]=2, dp[12]=min(dp[11]+1, dp[8]+1, dp[3]+1)=min(3+1,2+1,3+1)=3 ✓

> **教学解法（原有模板保留）：** 用于理解本题核心写法；最终提交请使用页面顶部的精确 YXC 入口。

```cpp
class Solution {
public:
    bool check(int x) {
        int r = sqrt(x);
        return r * r == x;
    }

    int numSquares(int n) {
        if (check(n)) return 1;

        for (int a = 1; a <= n / a; a ++ )
            if (check(n - a * a))
                return 2;

        while (n % 4 == 0) n /= 4;
        if (n % 8 != 7) return 3;
        return 4;
    }
};
```
### 思路二：BFS 最短路径

将问题转化为图：每个数字 i 可以走到 i - j²（j² <= i）。求从 n 走到 0 的最短步数。

```cpp
class Solution {
public:
    int numSquares(int n) {
        vector<int> dp(n + 1, INT_MAX);
        dp[0] = 0;
        for (int x = 1; x * x <= n; x ++)
            for (int sum = x * x; sum <= n; sum ++)
                dp[sum] = min(dp[sum], dp[sum - x * x] + 1);
        return dp[n];
    }
};
```
### 思路三：数学定理法（四平方和定理）

根据四平方和定理 + 勒让德三平方定理，可以 O(sqrt(n)) 判断。

```cpp
class Solution {
public:
    int numSquares(int n) {
        vector<int> dp(n + 1, INT_MAX);
        dp[0] = 0;
        for (int x = 1; x * x <= n; x ++)
            for (int sum = x * x; sum <= n; sum ++)
                dp[sum] = min(dp[sum], dp[sum - x * x] + 1);
        return dp[n];
    }
};
```
---

## ⚠️ 易错点

- **dp 数组大小**：dp 长度为 n+1，初始化 `dp[0]=0` 是关键基础。
- **内层循环范围**：`j*j <= i` 而不是 `j*j <= n`，否则会出现减出负数的情况。
- **inf 设置**：推荐 `INT_MAX` 或 `n + 1`，不要用 `10**9` 等可能导致相加溢出的值。
- **完全背包遍历顺序**：一维 dp 用正序遍历（与 0-1 背包区分）。

---

## 🔧 框架提炼

**完全背包求最小值模板：**

```cpp
class Solution {
public:
    bool check(int x) {
        int r = sqrt(x);
        return r * r == x;
    }

    int numSquares(int n) {
        if (check(n)) return 1;

        for (int a = 1; a <= n / a; a ++ )
            if (check(n - a * a))
                return 2;

        while (n % 4 == 0) n /= 4;
        if (n % 8 != 7) return 3;
        return 4;
    }
};
```
关键点：正序遍历容量（保证物品可重复取），dp[0] 初始化为 0，其余为 inf。

---

## 🔗 关联题目

- [[322-零钱兑换|322-零钱兑换]] — 完全背包的经典形式，思路完全一致
- [[416-分割等和子集|416-分割等和子集]] — 0-1 背包变体，注意与完全背包的区分
- [[139-单词拆分|139-单词拆分]] — 完全背包的字符串版本
