---
title: "322. 零钱兑换 (Medium)"
tags: [tech, reference]
created: 2026-07-15
type: permanent
summary: "C++17 Hot 100 项目中的《322. 零钱兑换 (Medium)》。"
---

# 322. 零钱兑换 (Medium)

> **专题归类：** [[10-动态规划|10-动态规划]]
> **LeetCode 题目：** [打开题目](https://leetcode.cn/problems/coin-change/)
> **AcWing 题目：** [打开题目](https://www.acwing.com/activity/content/problem/content/2713/)
> **AcWing yxc 代码：** [打开代码](https://www.acwing.com/activity/content/code/content/477128/)
> **YXC 最终提交版：** [[Hot100两周速通-yxc简洁代码#9. 零钱兑换（LeetCode 322 · Medium）|直达本题最终代码]]

---

## YXC 逐步动画 V2

![[archive/力扣刷题/04-Outputs/LeetCode动画-V2/试点/322-零钱兑换/Obsidian预览]]

---

## 题目描述

给你一个整数数组 coins，表示不同面额的硬币；以及一个整数 amount，表示总金额。

计算并返回可以凑成总金额所需的**最少的硬币个数**。如果没有任何一种硬币组合能组成总金额，返回 -1。

你可以认为每种硬币的数量是无限的。

**示例 1：**
```
输入：coins = [1, 2, 5], amount = 11
输出：3
解释：11 = 5 + 5 + 1
```

**示例 2：**
```
输入：coins = [2], amount = 3
输出：-1
```

**提示：**
- 1 <= coins.length <= 12
- 1 <= coins[i] <= 2^31 - 1
- 0 <= amount <= 10^4

---

## 🧩 题目详细分析

- **数据范围**：amount <= 10^4，硬币种类最多 12 种，适合 O(n * amount) 的 DP。
- **核心约束**：每种硬币无限使用（完全背包），求最少个数（不是组合数）。
- **边界条件**：amount = 0 时返回 0；如果硬币面额都大于 amount 则返回 -1。
- **隐藏条件**：可能存在无法凑出的金额，需返回 -1。硬币面额是无序的，注意排序不是必须的。

---

## 👶 小白版直白理解

你去商店买东西，需要凑够 amount 元。你口袋里有不同面额的硬币（每种无限多），你想用最少的硬币数付钱。比如要凑 11 元，你有 1 元、2 元、5 元硬币。你会先尽量用 5 元——两个 5 元是 10 元，再加一个 1 元，共 3 个硬币。这就像贪心解法，但注意有些面额组合贪心不一定最优（比如 coins=[1,3,4], amount=6，贪心用 4+1+1 共 3 个，但实际 3+3 只要 2 个），所以需要动态规划。

---

## 💡 解题思路

### 思路一：完全背包 DP（教学优先）

**核心洞察**：对于每个金额 j，如果选择面额 coin 的硬币，则问题变为凑 j-coin 的最少硬币数 + 1。在所有 coin 中取最小值。

**DP 五步法：**
1. **状态定义**：`f[j]` 表示凑成金额 `j` 所需的最少硬币数
2. **递推公式**：`f[j] = min(f[j], f[j - v] + 1)`，其中 `v` 是当前硬币面额
3. **初始化**：`f[0] = 0`，其余位置严格按照 YXC 代码初始化为 `1e8`
4. **遍历顺序**：先遍历硬币 `v`，再从 `j=v` 到 `m` 正序遍历金额，这是完全背包允许重复使用当前硬币的关键
5. **举例验证**：`coins=[1,2,5], m=11`，最终 `f[11]=3`，对应 `5+5+1`

> **教学解法（原有模板保留）：** 用于理解本题核心写法；最终提交请使用页面顶部的精确 YXC 入口。

```cpp
class Solution {
public:
    int coinChange(vector<int>& coins, int m) {
        vector<int> f(m + 1, 1e8);
        f[0] = 0;
        for (auto v: coins)
            for (int j = v; j <= m; j ++ )
                f[j] = min(f[j], f[j - v] + 1);
        if (f[m] == 1e8) return -1;
        return f[m];
    }
};
```
### 思路二：暴力递归 + 备忘录（自顶向下）

每种面额选或不选，用 memo 记录已计算过的金额。

```cpp
class Solution {
public:
    int coinChange(vector<int>& coins, int amount) {
        vector<int> dp(amount + 1, amount + 1);
        dp[0] = 0;
        for (int coin : coins)
            for (int sum = coin; sum <= amount; sum ++)
                dp[sum] = min(dp[sum], dp[sum - coin] + 1);
        return dp[amount] > amount ? -1 : dp[amount];
    }
};
```
### 思路三：BFS 最短路径

将金额看成图中的节点，每次减去一个硬币面额视为一条边，求 amount 到 0 的最短路径。

```cpp
class Solution {
public:
    int coinChange(vector<int>& coins, int amount) {
        vector<int> dp(amount + 1, amount + 1);
        dp[0] = 0;
        for (int coin : coins)
            for (int sum = coin; sum <= amount; sum ++)
                dp[sum] = min(dp[sum], dp[sum - coin] + 1);
        return dp[amount] > amount ? -1 : dp[amount];
    }
};
```
---

## ⚠️ 易错点

- **返回 -1 的判断：** YXC 版本使用 `1e8` 作为不可达哨兵，最后严格检查 `f[m] == 1e8`。
- **m = 0：** `f[0]` 已初始化为 0，循环后直接返回 0。
- **硬币面额大于 m：** 内层循环从 `j=v` 开始，此时起点已经大于终点，当前硬币会自然跳过。
- **完全背包 vs 0-1 背包**：一维完全背包正序遍历，0-1 背包倒序遍历。顺序反了会导致结果错误。

---

## 🔧 框架提炼

**完全背包（求最小值）模板：**

```cpp
class Solution {
public:
    int coinChange(vector<int>& coins, int m) {
        vector<int> f(m + 1, 1e8);
        f[0] = 0;
        for (auto v: coins)
            for (int j = v; j <= m; j ++ )
                f[j] = min(f[j], f[j - v] + 1);
        if (f[m] == 1e8) return -1;
        return f[m];
    }
};
```
**完全背包（求组合数）模板与最小值模板的区别：**
- 最小值：`dp[j] = min(dp[j], dp[j - coin] + 1)`，初值 inf
- 组合数：`dp[j] += dp[j - coin]`，初值 dp[0] = 1

---

## 🔗 关联题目

- [[279-完全平方数|279-完全平方数]] — 完全平方数就是特殊面额的"硬币"，思路完全一致
- [[416-分割等和子集|416-分割等和子集]] — 0-1 背包，区分完全背包与 0-1 背包的遍历顺序差异
- [[139-单词拆分|139-单词拆分]] — 完全背包的字符串版本，dp 类型变为布尔值
