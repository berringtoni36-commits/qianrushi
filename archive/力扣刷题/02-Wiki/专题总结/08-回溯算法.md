---
title: "08 · 回溯算法"
tags: [tech, reference]
created: 2026-07-15
type: permanent
summary: "C++17 Hot 100 项目中的《08 · 回溯算法》。"
---

# 08 · 回溯算法

> **来源：** labuladong 回溯三要素 + 代码随想录回溯模板 + 剪枝优化  
> **核心价值：** 回溯 = DFS + 撤销操作，本质是**暴力穷举的优雅写法**  
> **题量：** 8 题

## Hot100 题目链接与最终答案

> 本专题的 C++ 代码块用于讲解通用方法；每道题的最终提交版统一以 YXC 简洁代码为准。下面同时保留 LeetCode 和 AcWing 入口。

- **46. 全排列**：LeetCode [题目](https://leetcode.cn/problems/permutations/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2383/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/356268/) · [[46-全排列|题目详解]]
- **78. 子集**：LeetCode [题目](https://leetcode.cn/problems/subsets/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2427/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/370434/) · [[78-子集|题目详解]]
- **17. 电话号码的字母组合**：LeetCode [题目](https://leetcode.cn/problems/letter-combinations-of-a-phone-number/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2350/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/346821/) · [[17-电话号码的字母组合|题目详解]]
- **39. 组合总和**：LeetCode [题目](https://leetcode.cn/problems/combination-sum/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2376/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/355108/) · [[39-组合总和|题目详解]]
- **22. 括号生成**：LeetCode [题目](https://leetcode.cn/problems/generate-parentheses/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2355/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/347825/) · [[22-括号生成|题目详解]]
- **79. 单词搜索**：LeetCode [题目](https://leetcode.cn/problems/word-search/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2428/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/370446/) · [[79-单词搜索|题目详解]]
- **131. 分割回文串**：LeetCode [题目](https://leetcode.cn/problems/palindrome-partitioning/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2501/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/400664/) · [[131-分割回文串|题目详解]]
- **51. N皇后**：LeetCode [题目](https://leetcode.cn/problems/n-queens/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2396/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/362151/) · [[51-N皇后|题目详解]]

---


## 一、本质理解

labuladong 对回溯的定位非常精辟：

> **回溯 = 决策树的遍历过程**

### 回溯三要素

| 要素 | 说明 | 代码体现 |
|------|------|----------|
| **路径** | 已经做出的选择 | `path` 列表 |
| **选择列表** | 当前可以做的选择 | `for` 循环遍历 |
| **结束条件** | 到达决策树底层 | `if` 判断后 `return` |

### 回溯 vs 其他算法

| 算法 | 关系 | 区别 |
|------|------|------|
| DFS | 回溯 = DFS + 状态重置 | DFS 只管遍历，回溯遍历后要撤销 |
| 暴力穷举 | 回溯 = 优雅的暴力 | 回溯用递归实现，结构清晰 |
| 动态规划 | 回溯 = DP 无记忆版本 | DP 有重叠子问题优化，回溯没有 |

---

## 二、标准模板

```cpp
vector<vector<int>> backtrackingTemplate(const vector<int>& nums) {
    vector<vector<int>> answer;
    vector<int> path;
    auto dfs = [&](auto&& self, int start) -> void {
        answer.push_back(path);  // 当前路径形成一个解
        for (int i = start; i < static_cast<int>(nums.size()); ++i) {
            path.push_back(nums[i]);  // 做选择
            self(self, i + 1);
            path.pop_back();          // 撤销选择
        };
    };
    dfs(dfs, 0);
    return answer;
}
```
### 三个关键点

| 关键点 | 说明 | 常见错误 |
|--------|------|----------|
| **保存路径** | C++ 的 `result.push_back(path)` 会复制当前 `vector` | 若保存引用或指针，后续回溯会修改历史结果 |
| **撤销操作** | 递归返回后必须把状态还原 | 忘记 pop 或忘记还原 visited 标记 |
| **剪枝** | 在递归前跳过无效的选择 | 漏掉剪枝条件导致大量无用计算 |

---

## 三、核心变体

### 变体 1：全排列（用 visited 排除已选）

```cpp
class Solution {
public:
    vector<vector<int>> permute(vector<int>& nums) {
        vector<vector<int>> ans;
        vector<int> path;
        vector<bool> used(nums.size());
        auto dfs = [&](auto&& self) -> void {
            if (path.size() == nums.size()) {
                ans.push_back(path);
                return;
            }
            for (int i = 0; i < static_cast<int>(nums.size()); ++i) {
                if (used[i]) continue;
                used[i] = true;
                path.push_back(nums[i]);
                self(self);
                path.pop_back();
                used[i] = false;
            }
        };
        dfs(dfs);
        return ans;
    }
};
```
### 变体 2：子集（用 start 控制不回头）

```cpp
class Solution {
public:
    vector<vector<int>> subsets(vector<int>& nums) {
        vector<vector<int>> ans;
        vector<int> path;
        auto dfs = [&](auto&& self, int start) -> void {
            ans.push_back(path);
            for (int i = start; i < static_cast<int>(nums.size()); ++i) {
                path.push_back(nums[i]);
                self(self, i + 1);
                path.pop_back();
            }
        };
        dfs(dfs, 0);
        return ans;
    }
};
```
### 变体 3：组合总和（可重复选 + 剪枝）

```cpp
class Solution {
public:
    vector<vector<int>> combinationSum(vector<int>& candidates, int target) {
        sort(candidates.begin(), candidates.end());
        vector<vector<int>> ans;
        vector<int> path;
        auto dfs = [&](auto&& self, int start, int remain) -> void {
            if (remain == 0) {
                ans.push_back(path);
                return;
            }
            for (int i = start; i < static_cast<int>(candidates.size()) && candidates[i] <= remain; ++i) {
                path.push_back(candidates[i]);
                self(self, i, remain - candidates[i]);  // 可重复选择当前数
                path.pop_back();
            }
        };
        dfs(dfs, 0, target);
        return ans;
    }
};
```
### 变体 4：单词搜索（网格上的回溯）

```cpp
class Solution {
public:
    bool exist(vector<vector<char>>& board, string word) {
        int m = board.size(), n = board[0].size();
        auto dfs = [&](auto&& self, int i, int j, int k) -> bool {
            if (k == word.size()) return true;
            if (i < 0 || i == m || j < 0 || j == n || board[i][j] != word[k]) return false;
            char c = board[i][j];
            board[i][j] = '#';
            bool ok = self(self, i + 1, j, k + 1) || self(self, i - 1, j, k + 1) ||
                      self(self, i, j + 1, k + 1) || self(self, i, j - 1, k + 1);
            board[i][j] = c;
            return ok;
        };
        for (int i = 0; i < m; i ++)
            for (int j = 0; j < n; j ++)
                if (dfs(dfs, i, j, 0)) return true;
        return false;
    }
};
```
### 变体 5：N 皇后（经典棋盘问题）

```cpp
class Solution {
public:
    vector<vector<string>> solveNQueens(int n) {
        vector<vector<string>> ans;
        vector<string> board(n, string(n, '.'));
        vector<bool> column(n), diagonal(2 * n), antiDiagonal(2 * n);
        auto dfs = [&](auto&& self, int row) -> void {
            if (row == n) {
                ans.push_back(board);
                return;
            }
            for (int col = 0; col < n; ++col) {
                int d = row - col + n, a = row + col;
                if (column[col] || diagonal[d] || antiDiagonal[a]) continue;
                column[col] = diagonal[d] = antiDiagonal[a] = true;
                board[row][col] = 'Q';
                self(self, row + 1);
                board[row][col] = '.';
                column[col] = diagonal[d] = antiDiagonal[a] = false;
            }
        };
        dfs(dfs, 0);
        return ans;
    }
};
```
---

## 四、排列 vs 组合 vs 子集

| 问题 | 使用方式 | 去重方式 | 结果数量 |
|------|----------|----------|----------|
| **排列** | 每次从所有元素中选 | `visited` 标记已选 | n! |
| **组合** | 每次从 start 之后选 | `start` 参数控制 | C(n,k) |
| **子集** | 每次从 start 之后选 | `start` 参数控制 | 2^n |

**含重复元素时的去重技巧（排序+跳过同层重复）：**

```cpp
class Solution {
public:
    vector<vector<int>> subsetsWithDup(vector<int>& nums) {
        sort(nums.begin(), nums.end());
        vector<vector<int>> ans;
        vector<int> path;
        auto dfs = [&](auto&& self, int start) -> void {
            ans.push_back(path);
            for (int i = start; i < static_cast<int>(nums.size()); ++i) {
                if (i > start && nums[i] == nums[i - 1]) continue;
                path.push_back(nums[i]);
                self(self, i + 1);
                path.pop_back();
            }
        };
        dfs(dfs, 0);
        return ans;
    }
};
```
---

## 五、Hot 100 回溯题目清单

| 题号 | 题目 | 难度 | 核心技巧 | 建议用时 |
|------|------|------|----------|----------|
| 46 | 全排列 | Medium | visited 标记 | 25 min |
| 78 | 子集 | Medium | start 参数 | 25 min |
| 17 | 电话号码组合 | Medium | 逐位组合 | 25 min |
| 39 | 组合总和 | Medium | 排序剪枝 | 30 min |
| 22 | 括号生成 | Medium | 左右括号计数 | 30 min |
| 79 | 单词搜索 | Medium | 网格回溯 | 35 min |
| 131 | 分割回文串 | Medium | 切割+回文判断 | 35 min |
| 51 | N 皇后 | Hard | 对角线标记 | 40 min |

---

## 六、易错点与技巧

1. **深拷贝问题**：`res.push_back(path[:])` 而非 `res.push_back(path)`，这是最常见的 bug
2. **剪枝是灵魂**：回溯不剪枝 = 暴力穷举，剪枝后效率大幅提升
3. **排序预处理**：组合总和、含重复元素的排列/组合，先排序
4. **撤销要彻底**：做了什么选择，一定要在递归后还原
5. **选择顺序**：排列关注顺序（visited），组合不关注（start）

---

## 七、复杂度总结

| 问题类型 | 时间复杂度 | 空间复杂度 |
|----------|-----------|-----------|
| 全排列（无重复） | O(n × n!) | O(n) |
| 子集 | O(n × 2^n) | O(n) |
| 组合总和 | O(指数级) | O(target) |
| N 皇后 | O(n!) | O(n) |

---

## 八、参考与延伸

- [[07-图论|07-图论]]（回溯 = DFS + 撤销操作）
- [[10-动态规划|10-动态规划]]（DP 是带记忆化的回溯）
- labuladong 回溯框架：https://labuladong.online/algo/
- 代码随想录回溯专题：https://programmercarl.com/

---

## YXC 最终提交版直达

> 以下只负责跳转到唯一最终答案源；本专题原有教学模板和替代解法继续保留。

- [[Hot100两周速通-yxc简洁代码#1. 全排列（LeetCode 46 · Medium）|46. 全排列 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#2. 子集（LeetCode 78 · Medium）|78. 子集 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#3. 电话号码的字母组合（LeetCode 17 · Medium）|17. 电话号码的字母组合 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#4. 组合总和（LeetCode 39 · Medium）|39. 组合总和 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#5. 括号生成（LeetCode 22 · Medium）|22. 括号生成 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#6. 单词搜索（LeetCode 79 · Medium）|79. 单词搜索 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#7. 分割回文串（LeetCode 131 · Medium）|131. 分割回文串 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#8. N 皇后（LeetCode 51 · Hard）|51. N 皇后 · YXC 最终代码]]
