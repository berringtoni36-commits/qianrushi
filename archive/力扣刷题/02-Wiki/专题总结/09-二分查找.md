---
title: "09 · 二分查找"
tags: [tech, reference]
created: 2026-07-15
type: permanent
summary: "C++17 Hot 100 项目中的《09 · 二分查找》。"
---

# 09 · 二分查找

> **来源：** labuladong 二分查找统一模板 + 代码随想录循环不变量  
> **核心价值：** 在有序空间中用 O(log n) 找到目标，每次排除一半数据  
> **题量：** 6 题

## Hot100 题目链接与最终答案

> 本专题的 C++ 代码块用于讲解通用方法；每道题的最终提交版统一以 YXC 简洁代码为准。下面同时保留 LeetCode 和 AcWing 入口。

- **35. 搜索插入位置**：LeetCode [题目](https://leetcode.cn/problems/search-insert-position/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2372/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/355047/) · [[35-搜索插入位置|题目详解]]
- **74. 搜索二维矩阵**：LeetCode [题目](https://leetcode.cn/problems/search-a-2d-matrix/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2423/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/370359/) · [[74-搜索二维矩阵|题目详解]]
- **34. 在排序数组中查找元素首末位置**：LeetCode [题目](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2371/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/355034/) · [[34-在排序数组中查找元素首末位置|题目详解]]
- **33. 搜索旋转排序数组**：LeetCode [题目](https://leetcode.cn/problems/search-in-rotated-sorted-array/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2370/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/355022/) · [[33-搜索旋转排序数组|题目详解]]
- **153. 寻找旋转排序数组中的最小值**：LeetCode [题目](https://leetcode.cn/problems/find-minimum-in-rotated-sorted-array/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2534/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/411020/) · [[153-寻找旋转排序数组中的最小值|题目详解]]
- **4. 寻找两个正序数组的中位数**：LeetCode [题目](https://leetcode.cn/problems/median-of-two-sorted-arrays/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2329/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/339893/) · [[4-寻找两个正序数组的中位数|题目详解]]

---


## 一、本质理解

labuladong 对二分查找的定位：

> **二分查找 = 在有序空间中暴力穷举的优化版。**  
> 每次通过比较中间值，排除一半的搜索空间。

### 使用二分查找的关键条件

1. **有序性**：搜索空间（或其某种性质）是单调的
2. **可随机访问**：能用 O(1) 时间获取中间位置的元素
3. **可排除一半**：能通过一次比较，确定目标在左半还是右半

---

## 二、核心模板

### 标准二分查找

```cpp
class Solution {
public:
    int searchInsert(vector<int>& nums, int target) {
        return lower_bound(nums.begin(), nums.end(), target) - nums.begin();
    }
};
```
### 寻找左侧边界（第一个 >= target 的位置）

```cpp
int firstGreaterEqual(const vector<int>& nums, int target) {
    int left = 0, right = static_cast<int>(nums.size());
    while (left < right) {
        int mid = left + (right - left) / 2;
        if (nums[mid] >= target) right = mid;
        else left = mid + 1;
    }
    return left;
}
```
### 寻找右侧边界（最后一个 <= target 的位置）

```cpp
int lastLessEqual(const vector<int>& nums, int target) {
    int left = -1, right = static_cast<int>(nums.size());
    while (left < right) {
        int mid = left + (right - left + 1) / 2;
        if (mid < static_cast<int>(nums.size()) && nums[mid] <= target) left = mid;
        else right = mid - 1;
    }
    return left;
}
```
### 三种模板对比

| 模板 | 查找目标 | while 条件 | mid 比较 | 返回值 |
|------|----------|-----------|----------|--------|
| 标准 | 精确值 | left <= right | == 直接返回 | 索引或 -1 |
| 左边界 | 第一个 >= target | left <= right | >= 时收缩 right | left |
| 右边界 | 最后一个 <= target | left <= right | <= 时收缩 left | right |

---

## 三、变体应用

### 搜索旋转排序数组

```cpp
class Solution {
public:
    int search(vector<int>& nums, int target) {
        int left = 0, right = nums.size() - 1;
        while (left <= right) {
            int mid = left + (right - left) / 2;
            if (nums[mid] == target) return mid;
            if (nums[left] <= nums[mid]) {
                if (nums[left] <= target && target < nums[mid]) right = mid - 1;
                else left = mid + 1;
            } else {
                if (nums[mid] < target && target <= nums[right]) left = mid + 1;
                else right = mid - 1;
            }
        }
        return -1;
    }
};
```
### 寻找旋转排序数组最小值

```cpp
int findMin(const vector<int>& nums) {
    int left = 0, right = static_cast<int>(nums.size()) - 1;
    while (left < right) {
        int mid = left + (right - left) / 2;
        if (nums[mid] > nums[right]) left = mid + 1;
        else right = mid;
    }
    return nums[left];
}
```
### 搜索二维矩阵（将矩阵视为一维数组）

```cpp
class Solution {
public:
    bool searchMatrix(vector<vector<int>>& matrix, int target) {
        int m = matrix.size(), n = matrix[0].size();
        int left = 0, right = m * n - 1;
        while (left <= right) {
            int mid = left + (right - left) / 2;
            int x = matrix[mid / n][mid % n];
            if (x == target) return true;
            if (x < target) left = mid + 1;
            else right = mid - 1;
        }
        return false;
    }
};
```
### 寻找两个正序数组的中位数（Hard）

```cpp
class Solution {
public:
    double findMedianSortedArrays(vector<int>& a, vector<int>& b) {
        if (a.size() > b.size()) return findMedianSortedArrays(b, a);
        int m = a.size(), n = b.size(), left = 0, right = m;
        while (left <= right) {
            int i = (left + right) / 2;
            int j = (m + n + 1) / 2 - i;
            int aL = i ? a[i - 1] : INT_MIN;
            int aR = i < m ? a[i] : INT_MAX;
            int bL = j ? b[j - 1] : INT_MIN;
            int bR = j < n ? b[j] : INT_MAX;
            if (aL <= bR && bL <= aR) {
                if ((m + n) & 1) return max(aL, bL);
                return (max(aL, bL) + (double) min(aR, bR)) / 2;
            }
            if (aL > bR) right = i - 1;
            else left = i + 1;
        }
        return 0;
    }
};
```
---

## 四、Hot 100 二分查找题目清单

| 题号 | 题目 | 难度 | 核心技巧 | 建议用时 |
|------|------|------|----------|----------|
| 35 | 搜索插入位置 | Easy | 左边界二分 | 20 min |
| 74 | 搜索二维矩阵 | Medium | 二维转一维二分 | 25 min |
| 34 | 查找元素首末位置 | Medium | 左右边界两次二分 | 30 min |
| 33 | 搜索旋转排序数组 | Medium | 判断有序区间 | 35 min |
| 153 | 寻找旋转最小值 | Medium | 比较 mid 和 right | 30 min |
| 4 | 两个正序数组中位数 | Hard | 在短数组上二分分割线 | 45 min |

---

## 五、易错点与技巧

1. **循环条件**：`left <= right`（闭区间）vs `left < right`（开区间），保持一致即可
2. **mid 计算**：`mid = left + (right - left) // 2` 防溢出
3. **边界收缩**：关键是确定收缩后新区间是否包含 mid
4. **旋转数组**：先判断 mid 在哪段有序区间，再判断 target 的位置
5. **答案二分**：有时候不是对数组二分，而是对答案的值域二分（如求最大值最小化问题）

---

## 六、复杂度总结

| 场景 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| 标准二分 | O(log n) | O(1) |
| 旋转数组 | O(log n) | O(1) |
| 二维矩阵二分 | O(log(mn)) | O(1) |
| 中位数问题 | O(log min(m,n)) | O(1) |

---

## 七、参考与延伸

- [[03-数组与矩阵|03-数组与矩阵]]（搜索二维矩阵的两种方法）
- C++ `lower_bound` 模块：`lower_bound_left` 和 `lower_bound_right` 是二分查找的内置实现
- labuladong 二分查找详解：https://labuladong.online/algo/
- 代码随想录二分专题：https://programmercarl.com/

## YXC 模板精编

### 找第一个满足条件的位置

```cpp
template<class Check>
int firstTrue(int left, int right, Check check) {
    while (left < right) {
        int mid = left + (right - left) / 2;
        if (check(mid)) right = mid;
        else left = mid + 1;
    }
    return left;
}
```

### 找最后一个满足条件的位置

```cpp
template<class Check>
int lastTrue(int left, int right, Check check) {
    while (left < right) {
        int mid = left + (right - left + 1) / 2;
        if (check(mid)) left = mid;
        else right = mid - 1;
    }
    return left;
}
```

- **适用题：** 4、33、34、35、74、153。
- **复杂度：** 时间 `O(log n)`，空间 `O(1)`。
- **易错点：** 更新 `left = mid` 时必须使用上取整中点，否则会死循环；先定义答案满足的单调性质，再选择模板。

---

## YXC 最终提交版直达

> 以下只负责跳转到唯一最终答案源；本专题原有教学模板和替代解法继续保留。

- [[Hot100两周速通-yxc简洁代码#1. 搜索插入位置（LeetCode 35 · Easy）|35. 搜索插入位置 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#2. 搜索二维矩阵（LeetCode 74 · Medium）|74. 搜索二维矩阵 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#3. 在排序数组中查找元素的第一个和最后一个位置（LeetCode 34 · Medium）|34. 在排序数组中查找元素的第一个和最后一个位置 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#4. 搜索旋转排序数组（LeetCode 33 · Medium）|33. 搜索旋转排序数组 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#5. 寻找旋转排序数组中的最小值（LeetCode 153 · Medium）|153. 寻找旋转排序数组中的最小值 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#6. 寻找两个正序数组的中位数（LeetCode 4 · Hard）|4. 寻找两个正序数组的中位数 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#1. 无重复字符的最长子串（LeetCode 3 · Medium）|3. 无重复字符的最长子串 · YXC 最终代码]]
