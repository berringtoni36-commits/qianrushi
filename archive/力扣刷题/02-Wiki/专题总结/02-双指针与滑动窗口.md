---
title: "02 · 双指针与滑动窗口"
tags: [tech, reference]
created: 2026-07-15
type: permanent
summary: "C++17 Hot 100 项目中的《02 · 双指针与滑动窗口》。"
---

# 02 · 双指针与滑动窗口

> **来源：** labuladong 双指针框架 + 滑动窗口"三问法" + 代码随想录双指针专题  
> **核心价值：** 将 O(n²) 暴力解优化到 O(n)

## Hot100 题目链接与最终答案

> 本专题的 C++ 代码块用于讲解通用方法；每道题的最终提交版统一以 YXC 简洁代码为准。下面同时保留 LeetCode 和 AcWing 入口。

- **283. 移动零**：LeetCode [题目](https://leetcode.cn/problems/move-zeroes/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2663/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/456460/) · [[283-移动零|题目详解]]
- **11. 盛最多水的容器**：LeetCode [题目](https://leetcode.cn/problems/container-with-most-water/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2344/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/346694/) · [[11-盛最多水的容器|题目详解]]
- **15. 三数之和**：LeetCode [题目](https://leetcode.cn/problems/3sum/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2348/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/346791/) · [[15-三数之和|题目详解]]
- **42. 接雨水**：LeetCode [题目](https://leetcode.cn/problems/trapping-rain-water/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2379/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/356196/) · [[42-接雨水|题目详解]]
- **3. 无重复字符的最长子串**：LeetCode [题目](https://leetcode.cn/problems/longest-substring-without-repeating-characters/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2328/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/339855/) · [[3-无重复字符的最长子串|题目详解]]
- **239. 滑动窗口最大值**：LeetCode [题目](https://leetcode.cn/problems/sliding-window-maximum/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2633/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/445221/) · [[239-滑动窗口最大值|题目详解]]
- **76. 最小覆盖子串**：LeetCode [题目](https://leetcode.cn/problems/minimum-window-substring/) · AcWing [题目](https://www.acwing.com/activity/content/problem/content/2425/) · AcWing [yxc代码](https://www.acwing.com/activity/content/code/content/370411/) · [[76-最小覆盖子串|题目详解]]

---


## 一、本质理解

双指针的核心思想是 **利用两个指针的相对运动来减少搜索空间**。

labuladong 的双指针分类：

```
双指针
├── 左右指针（对撞）：两端向中间移动 → 有序数组、回文串
├── 快慢指针（同向）：一快一慢 → 链表判环、找中点
└── 滑动窗口（同向）：维护一个区间 → 子串/子数组问题
```

**延伸理解：** 滑动窗口本质上是 **快慢指针的一种特殊形式**，特殊在窗口内的元素始终是连续的（子数组/子串）。

---

## 二、对撞指针（左右指针）

### 核心思路

两个指针分别从数组的两端出发，向中间移动，直到相遇。

**适用条件：** 数组通常是有序的（或经过排序处理）。

### 标准模板

```cpp
bool twoPointerTemplate(const vector<int>& nums, int target) {
    int left = 0, right = static_cast<int>(nums.size()) - 1;
    while (left < right) {
        int sum = nums[left] + nums[right];
        if (sum == target) return true;
        if (sum < target) left ++;
        else right --;
    }
    return false;
}
```
### 三数之和（排序 + 对撞指针）

```cpp
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        sort(nums.begin(), nums.end());
        vector<vector<int>> ans;
        for (int i = 0; i + 2 < nums.size(); i ++) {
            if (i && nums[i] == nums[i - 1]) continue;
            if (nums[i] + nums[i + 1] + nums[i + 2] > 0) break;
            if (nums[i] + nums[nums.size() - 2] + nums.back() < 0) continue;
            int left = i + 1, right = nums.size() - 1;
            while (left < right) {
                int sum = nums[i] + nums[left] + nums[right];
                if (sum < 0) left ++;
                else if (sum > 0) right --;
                else {
                    ans.push_back({nums[i], nums[left], nums[right]});
                    for (left ++; left < right && nums[left] == nums[left - 1]; left ++);
                    for (right --; left < right && nums[right] == nums[right + 1]; right --);
                }
            }
        }
        return ans;
    }
};
```
---

## 三、快慢指针

### 核心思路

两个指针从同一起点出发，一个走的快（如每次 2 步），一个走的慢（如每次 1 步）。

### 模板 1：链表判环

```cpp
class Solution {
public:
    bool hasCycle(ListNode* head) {
        ListNode *slow = head, *fast = head;
        while (fast && fast->next) {
            slow = slow->next;
            fast = fast->next->next;
            if (slow == fast) return true;
        }
        return false;
    }
};
```
### 模板 2：找环入口（Floyd 算法）

```cpp
class Solution {
public:
    ListNode* detectCycle(ListNode* head) {
        ListNode *slow = head, *fast = head;
        do {
            if (!fast || !fast->next) return nullptr;
            slow = slow->next;
            fast = fast->next->next;
        } while (slow != fast);
        while (slow != head) slow = slow->next, head = head->next;
        return head;
    }
};
```
### 模板 3：找链表中点

```cpp
ListNode* middleNode(ListNode* head) {
    ListNode *slow = head, *fast = head;
    while (fast && fast->next) {
        slow = slow->next;
        fast = fast->next->next;
    }
    return slow;
}
```
---

## 四、滑动窗口 ⭐（高频核心）

### labuladong 三问判断法

滑动窗口的核心是 **三个问题**：

```
问题一：什么时候扩大窗口？
→ 当前窗口还不满足"可行解"条件时，右指针右移扩大

问题二：什么时候缩小窗口？
→ 当前窗口已经满足"可行解"条件时，左指针右移缩小

问题三：什么时候更新答案？
→ 缩小窗口时（求最小）或扩大窗口时（求最大）
```

### 通用模板

```cpp
class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        int ans = 0, left = 0;
        unordered_map<char, int> last;
        for (int right = 0; right < s.size(); right ++) {
            if (last.count(s[right])) left = max(left, last[s[right]] + 1);
            last[s[right]] = right;
            ans = max(ans, right - left + 1);
        }
        return ans;
    }
};
```
### 模板应用：无重复字符的最长子串

```cpp
class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        int ans = 0, left = 0;
        unordered_map<char, int> last;
        for (int right = 0; right < s.size(); right ++) {
            if (last.count(s[right])) left = max(left, last[s[right]] + 1);
            last[s[right]] = right;
            ans = max(ans, right - left + 1);
        }
        return ans;
    }
};
```
### 模板应用：最小覆盖子串

```cpp
class Solution {
public:
    string minWindow(string s, string t) {
        if (s.size() < t.size()) return "";
        array<int, 128> cnt{};
        for (char c : t) ++cnt[c];
        int missing = t.size(), left = 0, start = 0, len = INT_MAX;
        for (int right = 0; right < s.size(); right ++) {
            if (cnt[s[right]]-- > 0) missing --;
            while (missing == 0) {
                if (right - left + 1 < len) start = left, len = right - left + 1;
                if (++cnt[s[left ++]] > 0) missing ++;
            }
        }
        return len == INT_MAX ? "" : s.substr(start, len);
    }
};
```
---

## 五、Hot 100 双指针/滑动窗口题目清单

### 双指针（对撞）

| 题号 | 题目 | 难度 | 核心思路 | 建议用时 |
|------|------|------|----------|----------|
| 283 | 移动零 | Easy | 快慢指针，非零前移 | 25 min |
| 11 | 盛最多水的容器 | Medium | 对撞指针，移动较短的 | 30 min |
| 15 | 三数之和 | Medium | 排序 + 对撞指针 + 去重 | 40 min |
| 42 | 接雨水 | Hard | 对撞指针，维护左右最大高 | 40 min |

### 滑动窗口

| 题号 | 题目 | 难度 | 核心思路 | 建议用时 |
|------|------|------|----------|----------|
| 3 | 无重复最长子串 | Medium | 滑动窗口 + 哈希集 | 30 min |
| 438 | 找所有字母异位词 | Medium | 定长滑动窗口 + 计数 | 35 min |
| 239 | 滑动窗口最大值 | Hard | 单调递减队列 | 40 min |
| 76 | 最小覆盖子串 | Hard | 滑动窗口 + 需求计数 | 45 min |

---

## 六、易错点与技巧

1. **滑动窗口 vs 双指针**：滑动窗口解决"连续子区间"问题，双指针解决"两个元素的关系"问题
2. **滑动窗口的边界**：`right - left` 是窗口长度（右开区间），`right - left + 1` 是闭区间长度
3. **去重技巧**：三数之和中的去重用 `while` 跳过相邻重复值
4. **接雨水的核心**：当前位置能接的水 = `min(左边最高, 右边最高) - 当前高度`
5. **单调队列**：滑动窗口最大值用双端队列 `std::deque`，维护递减顺序

---

## 七、复杂度总结

| 模式 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| 对撞指针 | O(n) | O(1) |
| 快慢指针 | O(n) | O(1) |
| 滑动窗口 | O(n) | O(k)，k 为字符集大小 |
| 排序 + 双指针 | O(n log n) | O(1) 或 O(n) |

---

## 八、参考与延伸

- [[01-哈希表|01-哈希表]]（滑动窗口常用哈希表辅助）
- [[04-链表|04-链表]]（快慢指针在链表中应用更广）
- labuladong 滑动窗口框架：https://labuladong.online/algo/
- 代码随想录双指针专题：https://programmercarl.com/

## YXC 模板精编

### 同向双指针

YXC 原型是“右指针扩张，条件不满足时移动左指针”。力扣版把窗口状态封装在计数数组中。

```cpp
int longestUniqueWindow(const string& s) {
    array<int, 128> count{};
    int answer = 0;
    for (int left = 0, right = 0; right < static_cast<int>(s.size()); ++right) {
        ++count[static_cast<unsigned char>(s[right])];
        while (count[static_cast<unsigned char>(s[right])] > 1)
            --count[static_cast<unsigned char>(s[left++])];
        answer = max(answer, right - left + 1);
    }
    return answer;
}
```

### 对撞双指针

```cpp
int maxContainerArea(const vector<int>& height) {
    int left = 0, right = static_cast<int>(height.size()) - 1;
    long long answer = 0;
    while (left < right) {
        answer = max(answer, 1LL * min(height[left], height[right]) * (right - left));
        if (height[left] <= height[right]) ++left;
        else --right;
    }
    return static_cast<int>(answer);
}
```

- **适用题：** 3、11、15、42、76、283、438。
- **复杂度：** 两个指针都只单向移动，通常为 `O(n)`；三数之和外层再枚举一次，为 `O(n²)`。
- **易错点：** 窗口收缩条件必须由题目不变量决定；字符作数组下标时转换为 `unsigned char`。

---

## YXC 最终提交版直达

> 以下只负责跳转到唯一最终答案源；本专题原有教学模板和替代解法继续保留。

- [[Hot100两周速通-yxc简洁代码#4. 移动零（LeetCode 283 · Easy）|283. 移动零 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#5. 盛最多水的容器（LeetCode 11 · Medium）|11. 盛最多水的容器 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#6. 三数之和（LeetCode 15 · Medium）|15. 三数之和 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#7. 接雨水（LeetCode 42 · Hard）|42. 接雨水 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#1. 无重复字符的最长子串（LeetCode 3 · Medium）|3. 无重复字符的最长子串 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#2. 找到字符串中所有字母异位词（LeetCode 438 · Medium）|438. 找到字符串中所有字母异位词 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#4. 滑动窗口最大值（LeetCode 239 · Hard）|239. 滑动窗口最大值 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#5. 最小覆盖子串（LeetCode 76 · Hard）|76. 最小覆盖子串 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#1. 两数之和（LeetCode 1 · Easy）|1. 两数之和 · YXC 最终代码]]
- [[Hot100两周速通-yxc简洁代码#6. 寻找两个正序数组的中位数（LeetCode 4 · Hard）|4. 寻找两个正序数组的中位数 · YXC 最终代码]]
