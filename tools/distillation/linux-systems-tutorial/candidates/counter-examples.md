# Linux 教程反例候选

- `le01` 把 `-L` 当成运行时库搜索路径：会出现编译通过但启动时报找不到 `.so`；机制是链接期和动态加载期由不同搜索规则负责。
- `le02` 把一次 `read` 当成一次完整消息：TCP 是字节流，短读、合并读和拆分读都合法。
- `le03` 只给共享变量加“看起来够大”的锁：若读—改—写没有整体进入临界区，竞态仍在；若把阻塞 I/O 放进锁，吞吐会下降。
- `le04` 收到 epoll 可读事件后无限假设数据可读：EOF、错误、非阻塞 `EAGAIN` 和对端半关闭都必须处理。

