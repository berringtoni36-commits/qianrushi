{
  "skill": "rtos-runtime-fault-diagnosis",
  "version": "0.1.0",
  "darwin_compatible": true,
  "test_cases": [
    {"id":"should-trigger-01","type":"should_trigger","prompt":"STM32 一启动 FreeRTOS 就 HardFault，现在只有死循环，应该先保存哪些现场、怎么判断 PC 和 LR？","expected_behavior":"激活本 skill，先给 fault frame/SCB 寄存器取证和按 PC 分类的步骤","notes":"HardFault 现场"},
    {"id":"should-trigger-02","type":"should_trigger","prompt":"项目运行几分钟后偶发复位，怀疑任务栈溢出或 heap 不够，怎么用当前配置排查？","expected_behavior":"激活本 skill，指出当前栈溢出和 malloc 失败钩子关闭，并给出分阶段验证方案","notes":"栈堆诊断"},
    {"id":"should-trigger-03","type":"should_trigger","prompt":"TIM4 一开中断系统就崩，可能是信号量还没创建或者 IRQ 优先级不合法吗？","expected_behavior":"激活本 skill，核对句柄创建顺序、FromISR API 和 NVIC/FreeRTOS 阈值","notes":"初始化与中断"},
    {"id":"should-not-trigger-01","type":"should_not_trigger","prompt":"请解释这个项目的 Reset_Handler 到 vTaskStartScheduler 的完整启动链","expected_behavior":"不优先激活本 skill，转 rtos-freertos-config-and-boot","notes":"启动解释兄弟 Skill"},
    {"id":"should_not_trigger-02","type":"should_not_trigger","prompt":"DMA 完成中断触发了但 iap_task 没有醒，沿着信号量链路逐段排查","expected_behavior":"不优先激活本 skill，转 rtos-communication-debugging","notes":"通信事件兄弟 Skill"},
    {"id":"edge-01","type":"edge_case","prompt":"我只想要通用的 HardFault 定义，不需要排查我的项目或修改代码","expected_behavior":"做简短通用解释，不假设仓库当前实现，也不输出未经请求的修改方案","notes":"概念与诊断边界"}
  ],
  "minimum_pass_rate": 0.8
}
