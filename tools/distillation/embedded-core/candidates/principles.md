# 原则候选

- 先解释“为什么”，再背 API 名称。
- 把协议外设、物理电平和传输介质分开。
- 面试回答必须标明平台、版本和项目中的真实落点。
- C 标准的存储期/链接语义、工具链段布局、启动代码和当前 Map 必须分栏核对；不能用“全局变量一般在 BSS”替代证据。
- TCP 的 ACK、`ESTABLISHED` 和 `send()` 返回值只能证明各自层级的状态；应用交付和持久化要有独立确认。
