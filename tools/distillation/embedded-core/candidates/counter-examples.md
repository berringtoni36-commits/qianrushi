# 反例候选

- 把 UART 外设和 RS232 电平当成同一件事。
- 只说“用了 FreeRTOS”，却说不清任务、ISR 和共享数据如何协作。
- 把“系统没启动”直接归因于驱动，不看启动阶段证据。
- 把 `.bss`、`extern`、`volatile` 或 `ESTABLISHED` 当成跨工具链/跨层的万能结论。
- 把 TCP ACK 或 `send()` 成功直接说成应用消息已处理、落盘；把一次 `ping` 丢包直接当成 TCP 业务路径故障。
