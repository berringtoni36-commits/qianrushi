# LeetCode 反例候选

- `ae01` 只背模板不看约束：同一“二分”可能是下标边界、答案二分或旋转数组，不变量不同。
- `ae02` 从题解代码倒推 DP：变量名看懂但状态含义不清，遇到初始化、遍历方向或空间压缩就失效。
- `ae03` 把一个样例当贪心证明：局部选择看似合理，仍可能被构造的反例击穿。
- `ae04` 以“看过/提示后 AC”标记完成：没有无提示重写和间隔复习，掌握度会被高估。

