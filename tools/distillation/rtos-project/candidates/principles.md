# RTOS 原则候选

- ISR 不做长耗时业务。
- 互斥量保护共享数据，信号量表达事件。
- 任务优先级由响应期限和执行行为决定。
- IAP 必须说明宏开关、CRC32、Flash 地址和恢复路径。
- 启动顺序先创建同步对象，再启动可能释放它们的中断/定时器。
- HardFault 先取现场和 PC/LR，再做根因归类；不能用“加 delay”替代证据。
- 任务栈/堆大小必须结合 `StackType_t`、map 和高水位检查，不能只看宏注释。
- CRC32 证明传输完整性，不证明身份；单 APP 写入没有 A/B 回滚和断电一致性。
