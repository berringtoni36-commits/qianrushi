# RedNote 审计限制与开放队列

## 已确认的限制

1. **根级 status 只对当前快照负责。** 2026-08-14 10:22 CST 的根 `source-disposition.tsv` 已包含目标映射，当前 status 为 `domain-referenced=47`、`needs-domain-review=345`、`evidence-layer=1,560`，disposition 为 `derived-index=11`、`external-reference=202`、`needs-review=179`、`evidence-layer=1,560`。原始资料或来源映射发生变化后，需重新运行回归刷新快照。
2. **目录前缀不等于逐文件理解。** 根级 47 条 `domain-referenced` 以及未来可能增加的前缀回链都只证明路径边界，而不是正文内容、图片文字或外部事实已核验。
3. **Likes 尚无人工 external-reference 处置。** 177 条 Likes 的作者字段看起来是第三方，但当前覆盖表没有给 Likes 明确 `external-reference` 规则；本审计不擅自扩展全局规则。
4. **Posts 的身份边界未确认。** 2 条 `我的发布` 的 frontmatter 显示作者和账户名一致，但脱敏元数据不能独立证明用户身份、经历或授权转述。
5. **附件只做文件/哈希/引用审计。** 1,560 个媒体文件中有 96 个二重哈希组（192 行、96 行冗余）；图片/视频没有 OCR、内容语义核验或原始拍摄数据验证。
6. **媒体存在 2 个规范路径差集。** 正文引用了 `image-1/2 (Multi Source Sync).jpg`，而 inventory 另登记了对应的无后缀变体；这被保留为文件名/同步边界，不判断为内容缺失或内容相同之外的语义结论。
7. **外部 URL 未重新抓取。** 本轮只读本地 Markdown；没有联网确认帖子是否仍可访问、作者是否本人、产品版本/价格/Star/安装量是否变化。
8. **哈希去重不是语义去重。** 相同 SHA-256 只能说明字节相同；不同图片、转码视频、正文改写和相同帖子多份导出仍需人工判断关系。
9. **标签和关键词是信号，不是分类真值。** 主题摘要的标签数是出现次数，主题可重叠；标签可能来自作者营销、评论或同步格式，不能代替正文审阅。
10. **本报告没有创建规范 Skill。** RedNote 仍是外部参考域；候选方法与已有 Skill 的承接关系见 [`verified.md`](tools/distillation/rednote-bookmarks/verified.md) 和 [`rejected/not-skills.md`](not-skills.md)。

## 开放队列

| 队列 | 数量 | 完成条件 |
|---|---:|---|
| 根级快照刷新 | 已完成（2026-08-14 10:22 CST） | 后续若原始资料或来源映射变化，再运行回归并重新核对 status |
| Likes 人工处置 | 177 | 用户确认是否纳入外部参考，再决定是否新增最小规则 |
| Posts 身份/转述确认 | 2 | 用户确认账户归属、用途和是否可作为个人来源 |
| 规范媒体路径差集 | 2 | 确认两个无后缀媒体文件与 `Multi Source Sync` 变体的导出关系 |
| 图片/视频内容核验 | 1,560 行 | 有授权的 OCR/人工阅读、原始数据或外部主源，并逐项回链 |
| 外部事实复核 | 未定 | 对招聘、薪资、工具功能、健康/教育信息等按日期和原始出处独立核验 |

## 不应做的推断

- 不把 1,952 个文件登记写成 1,952 个文件已理解；
- 不把 391 个 `knowledge-document` 写成 391 条用户知识；
- 不把 202 收藏、177 点赞或 2 发布写成用户经历；
- 不把专辑索引、Base、图片或重复副本重复计数；
- 不把作者的“亲测”、offer、薪资、节省比例、Star、安装量和产品宣传语升级成事实；
- 本报告的 disposition 与 status 数字均对应 2026-08-14 10:22 CST 根级机器快照；这不是逐篇正文理解或外部事实核验的证明。
